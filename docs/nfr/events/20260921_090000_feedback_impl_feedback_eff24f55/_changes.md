# 変更サマリ

- event_id: 20260921_090000_feedback_impl_feedback_eff24f55
- trigger_event: rdra:20260921_084000_feedback_impl_feedback_eff24f55
- created_at: 2026-09-21T09:00:00Z
- mode: 差分更新(nfr-grade-diff.yaml)
- dialogue_policy: interactive(確認推奨項目 2 件を返し、2026-09-21 に利用者が確定。confidence: low の残件なし)
- feedback request: 20260919_213000_impl_feedback_eff24f55(direct work unit: CR-eff24f55-004#1。causal work unit: CR-eff24f55-004#1, CR-eff24f55-005#1。packet: docs/pipeline/feedback-runs/20260919_213000_impl_feedback_eff24f55/stage-packets/quality_attributes.md)

```yaml
feedback_request:
  feedback_request_id: "20260919_213000_impl_feedback_eff24f55"
  input_sha256: "1a7612598a0ed1d3378d7479b1614d6a6aa99ac90eae0fbf55b82e173784a431"
  request_ids: ["CR-eff24f55-004","CR-eff24f55-005"]
  work_unit_ids: ["CR-eff24f55-004#1","CR-eff24f55-005#1"]
```

マージ規則: メトリクス `id` をキーに latest/nfr-grade.yaml の該当メトリクスの `grade_description` / `reason` / `source_model` / `confidence` を上書きした。`grade` はすべて据え置き。更新前に `confidence: "user"` だったメトリクスは対象に含まれない。モデルシステム(model1)は変更なし。

## 追加

- なし

## 変更

| ID | メトリクス | Lv(据え置き) | 起因 CR | 変更内容 |
|---|---|---|---|---|
| B.1.1.2 | データ量 | 1 | CR-004 | 「CSV 形式の設定ファイルの共通前提」を 1 か所に定めた。対象は slot ジョブマップ・クロスチェックジョブマップ・対象カタログ。1 ファイルあたりの想定最大行数はヘッダー行を除いて 5,000 行。内容の最悪条件(全列に値、キー列が全行一意、版の列が全行異なる)と、行数に比例する時間で処理できる実装の前提を明記。機能ごとの仕様・検証はこの共通前提から条件を引く。confidence を medium → user に変更 |
| B.2.1.1 | レスポンスタイム | 2 | CR-004 | CSV 形式の設定ファイルを読む CLI と facade の設定読み込みは、B.1.1.2 の共通前提(想定最大 5,000 行、最悪条件を含め内容によらず)で 10 秒以内と定めた。行数と内容の条件は B.1.1.2 を参照し、ここでは再定義しない。confidence を medium → user に変更 |

## 削除

- なし

## 利用者の決定(2026-09-21)

| # | 決定 | 根拠 |
|---|---|---|
| 1 | 想定最大行数は 5,000 行(ヘッダー行を除く) | 1 つのジョブマップに 3,000 ジョブ程度はあり得るため、余裕を持たせる |
| 2 | 10 秒以内は「内容によらず」(最悪条件を明記) | 性能の合否を一意に判定できるようにする |
| 3 | 2 点は slot ジョブマップ専用にせず、同じ CSV 形式の設定ファイルに共通の前提として 1 か所に書く | 閾値を機能ごとに毎回考え直さない。十分な速度があれば問題にならない |

## 実測と実装への影響

2026-09-21 の実測(現状のジョブマップ検証。最悪条件 = 全列に値・job_id 全行一意・map_version 全行相違):

| 行数 | 最悪条件 | 典型(map_version 全行同一) |
|---|---|---|
| 1,000 行 | 2.2 秒 | - |
| 2,000 行 | 6.6 秒 | - |
| 3,000 行 | 13.8 秒 | 2.5 秒 |
| 5,000 行 | 36 秒 | - |

- 現状の実装は、5,000 行の最悪条件で 10 秒を超える。
- 原因は、map_version の distinct 集計が行数の 2 乗の比較になっている実装である。
- 行数に比例する処理へ改める実装改善が前提となる(仕様確定後に実装側で直す)。

## 照合結果

- CR-eff24f55-004(想定最大行数と、性能目標を満たす行数・内容): B.1.1.2 と B.2.1.1 に反映した。同じ要求の bash の最低版の明記は、非機能要求グレードの対象外(実装向けの仕様・規約の所有)のため、本 stage では扱わない。スループット(B.2.1.2)、ターンアラウンドタイム(B.2.1.3)、バッチ処理時間(B.2.2.1)は設定ファイルの行数に依存しないため据え置き
- CR-eff24f55-005(受け入れ基準の分割): 影響なし。USDM の受け入れ基準の文面の分割であり、RDRA モデルは不変。調整記録を適用構成文書に残す前提は、バックアップ対象(適用構成文書を含む)の記述と整合したままで、どのメトリクスの値・根拠も変わらない

## 仮採用(confidence: low)

- なし(0 件)
