schema_version: "1.0"
stage: "S7"
uc_id: "6078c4ed"
result: pass
integration_command: "npx cucumber-js --config cucumber.cjs --profile atdd --tags '@atdd_SPEC-001-01-1 or @atdd_SPEC-001-02-1 or @atdd_SPEC-001-03-1 or @atdd_SPEC-009-01-1 or @atdd_SPEC-009-02-1 or @atdd_SPEC-009-03-1'"
exit_code: 0
runs: "3 consecutive runs (the last one after adding the uc tag), each 6 scenarios / 6 passed, 30 steps / 30 passed"
selection:
  method: "exact tag match (cucumber --tags or-expression); no name-based partial filter; features/atdd is not run as a whole"
  tags:
    - "@atdd_SPEC-001-01-1"
    - "@atdd_SPEC-001-02-1"
    - "@atdd_SPEC-001-03-1"
    - "@atdd_SPEC-009-01-1"
    - "@atdd_SPEC-009-02-1"
    - "@atdd_SPEC-009-03-1"
  expected_scenarios: 6
  executed_scenarios: 6
  executed_scenario_names:
    - "SPEC-001-01-1"
    - "SPEC-001-02-1"
    - "SPEC-001-03-1"
    - "SPEC-009-01-1"
    - "SPEC-009-02-1"
    - "SPEC-009-03-1"
  count_matches_atdd_scenarios: true
scenarios: 6
scenarios_passed: 6
steps: 30
steps_passed: 30
features:
  - "features/atdd/SPEC-001-01.feature"
  - "features/atdd/SPEC-001-02.feature"
  - "features/atdd/SPEC-001-03.feature"
  - "features/atdd/SPEC-009-01.feature"
  - "features/atdd/SPEC-009-02.feature"
  - "features/atdd/SPEC-009-03.feature"
steps_file: "features/atdd/steps/select-and-launch-slots-by-feature-flags.steps.cjs"
tiers_integrated:
  - "tier-facade"
harness:
  rdb: "SQLite (limited verification boundary per issues/20260817T230000Z and issues/20260821T220045Z section 1; schema = contract columns + rdb-schema.yaml PK/UNIQUE)"
  launch: "ssh stub on PATH appends the launch argv to a log file; launches are attributed per CLI invocation by log offset"
  id_generation: "RELAYGATE_ID_GENERATOR seam (facade/src/id_gateway.sh) with fixed run_id and att-<slot>-0001 attempt_id"
  job_map: "JSON slot-entry format from facade/src/job_map_gateway.sh header comment; concrete values taken from spec.md E2E Given clauses"
harness_injections: []
scenario_verification:
  - tag: "@atdd_SPEC-001-01-1"
    verifies: "exit 0; blue launched as foreground and green as background (ssh target + RELAYGATE_ROLE); runner_results STARTING rows per slot; stdout lines per slot"
  - tag: "@atdd_SPEC-001-02-1"
    verifies: "two launches on the same job map (foreground/background/on then off/foreground/off) both exit 0; job map file unchanged; first run has blue+green, second run has green foreground only; same job_map_version across runs"
  - tag: "@atdd_SPEC-001-03-1"
    verifies: "exit 2 with the exclusive-foreground message; no launch events, no stdout, no rows in execution_specs / slot_execution_specs / runner_results / runner_result_events / audit_logs"
  - tag: "@atdd_SPEC-009-01-1"
    verifies: "scheduler input is JOB_ID + additional args only; execution_specs (job_id, job_map_version, hang_detect_limit_minutes, additional_args) and slot_execution_specs (host, exec_user, script_path, work_dir) resolved from the job map; launch target/work_dir/script/argv use the resolved values"
  - tag: "@atdd_SPEC-009-02-1"
    verifies: "fixed_args and additional_args persisted; blue and green launch argv equal fixed_args followed by additional args in order"
  - tag: "@atdd_SPEC-009-03-1"
    verifies: "execution spec persisted exactly once (1 execution_specs row, 1 slot_execution_specs row per slot, unique (run_id, slot_type)); all resolved fields present; no secret-bearing column; an injected credential secret in the job map appears nowhere in the RDB dump, launch events, or stdout; credential_ref only"
execution_spec_json_interpretation: "execution-spec.json (RDRA information) is persisted as execution_specs + slot_execution_specs per spec.md; verified on those tables (issue section 12)"
files_updated:
  - "features/atdd/steps/select-and-launch-slots-by-feature-flags.steps.cjs"
  - "features/atdd/SPEC-001-01.feature"
  - "features/atdd/SPEC-001-02.feature"
  - "features/atdd/SPEC-001-03.feature"
  - "features/atdd/SPEC-009-01.feature"
  - "features/atdd/SPEC-009-02.feature"
  - "features/atdd/SPEC-009-03.feature"
  - "docs/impl/latest/6078c4ed/issues/20260821T220045Z_select-slot-spec-reflow-test-boundary.md"
files_created:
  - "docs/impl/latest/6078c4ed/stages/S7_atdd.done.yaml"
feature_changes: "only the tag line of the 6 selected scenarios gained @uc_6078c4ed; Scenario names and step text are unchanged"
tier_code_changed: false
quality_checks:
  qlty: "qlty check --no-progress --no-formatters --fail-level medium features/atdd/steps/select-and-launch-slots-by-feature-flags.steps.cjs (No issues, exit 0)"
issues_appended:
  - path: "docs/impl/latest/6078c4ed/issues/20260821T220045Z_select-slot-spec-reflow-test-boundary.md"
    sections: "S7 atdd addendum: section 12 (SPEC-009-03 wording 'execution-spec.json' and 'per-role hang_detect_limit_minutes' vs RDB split storage with run-level limit), section 13 (how 'no credential secret' was verified), section 14 (uc tag on ATDD features)"
notes:
  - "all 6 scenarios pass on the real tier-facade behaviour without harness injection"
  - "acceptance criteria carry no concrete values; values were taken from spec.md E2E Given clauses (job map v1.4.0, daily-settlement, blue-host-01 / green-host-01, cred-*-batch)"
  - "cucumber-js 13.2.1, profile atdd, strict mode"
manifest_sha256: "836dfd9cbb6af8c8a7f63043c45bc2beffc5469d632e523cf27748859f89b1c2"
manifest_projection: v2
completed_at: "2026-08-21T23:51:00Z"
completed_by: "dist-impl-implement@0.11.1"
