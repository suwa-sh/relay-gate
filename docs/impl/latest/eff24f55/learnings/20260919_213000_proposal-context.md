# CLAUDE.md / dev-rules への追記提案(eff24f55 由来)

既存の CLAUDE.md と dev-rules は変更していない。採否は人が判断する。

## 提案 1: 設定ファイルを読む UC の実装前に守備範囲を確定する

- 追記先の候補: `CLAUDE.md` の「実装フェーズ(再開時)」、または `docs/dev-rules/coding-rules.md`
- 追記案:
  - 設定ファイルや外部入力を読む UC は、実装前に「入力の守備範囲」が仕様にあることを確認する。
  - 無ければ、distillery の差分更新で先に確定する。実装で決めない。
- 根拠: eff24f55 で独立検証が 4 回続けて同種の blocker を出した。

## 提案 2: 対象の bash の版を dev-rules に明記する

- 追記先の候補: `docs/dev-rules/coding-rules.md`
- 追記案: 対象の bash の最低版を明記する。インフラ設計(bash 5)と同じ値にする。
- 根拠: 実装者は版が分からず、版に依存しない構文を選んだ(連想配列を避けた)。
- 注意: 正本は仕様側の変更要求(CR-eff24f55-004)の結果に従う。

## 提案 3: bash の入力処理の規約

- 追記先の候補: `docs/dev-rules/coding-rules.md`
- 追記案:
  - 入力ファイルは 1 回だけ読み、すべての検査で同じ内容を使う。
  - プロセス置換の中のコマンドの失敗を前提にしない。結果は一時ファイル経由で受け、終了状態を確認する。
  - 正規表現は `LC_ALL=C` で評価する。
- 根拠: `learnings/20260919_213000_bash-input-pitfalls.md`
