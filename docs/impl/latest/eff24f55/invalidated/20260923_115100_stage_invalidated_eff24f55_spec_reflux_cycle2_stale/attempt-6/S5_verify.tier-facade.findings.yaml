schema_version: "1.0"
uc_id: "eff24f55"
tier: "tier-facade"
attempt: 6
verified_at: "2026-09-22T19:16:48+09:00"
gate_reexec:
  format: "pass"
  lint: "pass"
  tdd: "pass"
  bdd_tier: "pass"
viewpoints_checked:
  spec_conformance:
    status: "done"
    note: "CLI 引数・9列・出力・終了コードを突合。feature の全 Gherkin 行は仕様と一致。_api-summary は旧CLI形式で paths=[]、_model-summary は tables=[]。DB操作なし。F-003 は入力ソース間矛盾。"
  readability_maintainability:
    status: "done"
    note: "dev-rules の命名・AAA・層分割を照合。format/lint 通過。仕様参照コメントと定数、実ファイル・実プロセステストを確認。"
  security:
    status: "done"
    note: "CSV/JSON を評価実行せず解析。credential_ref を診断に出さない。NUL/不正UTF-8拒否、制御文字の可視化、スナップショット0600をテストで確認。"
  performance:
    status: "done"
    note: "NFR B.1.1.2/B.2.1.1 の最悪条件5000行を実測し10秒以内。job_idはsort、map_versionは連想配列。時間は実機での測定値で全環境を保証するものではない。"
  operability:
    status: "done"
    note: "方針資料のCSVを抽出し実行。督促AP・Windowsパス・相対パス・空白/カンマの固定引数の表示保持。失敗診断は利用者の入力パスを保持。"
  fault_tolerance:
    status: "done"
    note: "PATHの実失敗fixtureでtr/sed/sort/rm/cat/mktemp/mvを検査。F-001/F-002解消。Bats/BDDで単一スナップショット・HUP/INT/TERM削除を確認。BSD経路の残存はF-004。"
  refactoring:
    status: "done"
    note: "CSV/JSON短経路と窓処理の同文法、共有制御文字表記、usecaseの診断蓄積を確認。重複実装による具体的な契約違反は立証されなかった。"
  assumption_conformance:
    status: "done"
    note: "観点1〜7完走後にid/assumption/targetだけを読み、S4固定指示との照合後に判定を確定。全24件exactly once。分類差4件をminorで可視化。候補は記録に対応しunlistedなし。"
assumptions_sha256: "af1e2927136c7f7492f683b87da2c641501dd8cb13bc34bb7fcb304ead0d514a"
assumptions_hash_basis: "raw file bytes; shasum -a 256; user instruction overrides canonical record hash"
assumption_verdicts:
  - id: "A-005"
    tier: "tier-facade"
    assumption: "ヘッダー行がクォート不正のときは、ヘッダー行とデータ行の csv quote is invalid だけを全件報告し、ヘッダー検証と列検証を行わずに終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:151"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-005"
    evidence: "tier md UC ロジックはクォート不正を拒否するが、解析不能ヘッダー後のデータ行検証の範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-006"
    tier: "tier-facade"
    assumption: "ヘッダー不一致(必須列の欠落、host / user の片方だけ)のときは、データ行の列検証と job_id 重複検査を行わず、データ行の csv quote is invalid だけを追加で報告して終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:158"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-006"
    evidence: "必須列と host/user 対の拒否は明記。列対応不能時にクォート検証だけ続ける範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-008"
    tier: "tier-facade"
    assumption: "job map header mismatch の missing= に複数の列を並べるときは、契約の列順(job_id, host, user, work_dir, script, fixed_params, hang_detect_limit_minutes)で並べる"
    target: "facade/src/domain/job_map.sh:150"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-007"
    evidence: "契約の missing=<col,...> は複数欠落列の並び順を定めていない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-010"
    tier: "tier-facade"
    assumption: "csv quote is invalid または column count mismatch になったデータ行は、列ごとの検証、job_id 重複検査、map_version の集計の対象にしない"
    target: "facade/src/usecase/validate_job_map.sh:176"
    category: "error_handling"
    verified_category: "input_validation"
    verdict: "spec_absent"
    finding_id: "F-008"
    evidence: "クォート・列数の拒否は明記されるが、重複検査と版集計の母集団への包含は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-012"
    tier: "tier-facade"
    assumption: "credential_ref が参照名の形(英数字と _ . - だけ)に合わない値は、/ も BEGIN も含まなくても warn: credential_ref looks like a secret or path を出し、拒否はしない"
    target: "facade/src/domain/job_map.sh:281"
    category: "security"
    verified_category: "security"
    verdict: "spec_absent"
    finding_id: "F-009"
    evidence: "credential_ref の正規表現と /・BEGIN の warn は明示。その他の形式不一致を警告にするか拒否するかは明示されていない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-014"
    tier: "tier-facade"
    assumption: "info: resolved 行の fixed_params= は、解析後の要素を要素間に空白を置かない 1 行の JSON 配列に組み立て直し、スラッシュ(/)と U+00A0 以上の非 ASCII 文字はエスケープしない"
    target: "facade/src/domain/job_map.sh:420"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-010"
    evidence: "control_chars の JSON 表記は規定されるが、等価な JSON の要素間空白と任意エスケープの正規形は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-016"
    tier: "tier-facade"
    assumption: "hang_detect_limit_minutes が導入時の推奨値 60 と違う行に対する専用の info: 行は出さず、--verbose の info: resolved 行の hang_detect_limit_minutes=N で値を示すだけにする"
    target: "facade/src/domain/job_map.sh:509"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-011"
    evidence: "spec.md の推奨値との差を info とする記載に専用文言はなく、CLI 契約には verbose の resolved 行がある。専用行を省く選択は明示されない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-017"
    tier: "tier-facade"
    assumption: "map_version の distinct 値が複数あるとき、stdout の map_version= と warn: mixed map_version の values= には、distinct 値を初出順のカンマ区切りで出す"
    target: "facade/src/domain/job_map.sh:655"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-012"
    evidence: "spec.md 計算ルールは全行の distinct 値と混在警告を定義するが、初出順は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-018"
    tier: "tier-facade"
    assumption: "map_version が空の行と値のある行が混在するとき、空の値は warn: mixed map_version の values= と stdout の map_version= に空値表記 - で出す(例: 空の行と v1 の行なら values=-,v1 と map_version=-,v1)"
    target: "facade/src/domain/job_map.sh:655"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-013"
    evidence: "空値単体の - は ui-design にあるが、混在版リスト内の空要素への適用は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-019"
    tier: "tier-facade"
    assumption: "--verbose の info: resolved 行は列ごとの検証を通過した行にだけ出し、違反のある行には出さない(他の行に違反があって終了コード 2 になるときも、通過した行の分は出す)"
    target: "facade/src/usecase/validate_job_map.sh:180"
    category: "data_format"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-014"
    evidence: "契約 stderr は verbose 解決結果の形式を定義するが、違反行との混在時の出力範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-020"
    tier: "tier-facade"
    assumption: "stderr の行は、入力の守備範囲の error(行番号順。hint は最後の CR の次)、ヘッダーの unknown column の warn、duplicate column の error、header mismatch の error、データ行ごとの error と warn と info(行番号の昇順)、duplicate job_id、mixed map_version の順に、検証を終えてからまとめて出す"
    target: "facade/src/usecase/validate_job_map.sh:129"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-015"
    evidence: "全件報告と CR hint の位置以外に、異種の error/warn/info 間の全体順序は規定されない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-022"
    tier: "tier-facade"
    assumption: "job_id 重複検査は、job_id is invalid になった値も文字列(バイト列の完全一致)として比較の対象に含め、空の job_id だけを対象から外す"
    target: "facade/src/usecase/validate_job_map.sh:189"
    category: "input_validation"
    verified_category: "input_validation"
    verdict: "spec_absent"
    finding_id: "F-016"
    evidence: "job_id の形式と一意性は明示。不正値・空値を重複比較へ含める範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-025"
    tier: "tier-facade"
    assumption: "複数の job_id が重複しているとき、error: duplicate job_id の行は、その job_id が 1 回目に現れた行番号の昇順で出す"
    target: "facade/src/domain/job_map.sh:636"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-017"
    evidence: "複数 job_id の重複報告順は未規定。初出行を lines に含める別の本文/BDD矛盾は独立 finding とする。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-043"
    tier: "tier-facade"
    assumption: "検証を通過した行の解決結果(info: resolved)や版の集計を作れない内部障害は、補助コマンドの失敗と同じ error: internal command failed commands=<関数名> path: <path> の 1 行と終了コード 6 で終える(commands= に外部コマンドではなく関数名 job_map_row_resolved_line / job_map_version_summary を入れる)"
    target: "facade/src/usecase/validate_job_map.sh:183"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-018"
    evidence: "internal_failure は補助コマンドの失敗を規定するが、内部関数名を commands に載せる拡張は未規定。関数差替え後の正しさは対象外。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-044"
    tier: "tier-facade"
    assumption: "補助コマンドが終了状態 0 を返しても、次のどれかに当たるときは補助コマンドの失敗として扱う。sort の出力の行数が入力の行数と違う。sort の出力が job_id のバイト列の昇順に並んでいない。事前走査の出力に最終行まで走査した証跡(証跡の文字と総行数)が無い・想定外の文字がある。事前走査が数えた総行数と bash が読み込んだ行数が違う"
    target: "facade/src/domain/job_map.sh:605"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-019"
    evidence: "想定外出力の拒否は internal_failure に明示。行数・並び順・終端証跡という具体的検査方式は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-049"
    tier: "tier-facade"
    assumption: "ヘッダー列名の重複があるときは、duplicate column の error(重複した列名ごとに 1 行、初出順)に加えて必須列の欠落があれば header mismatch の error も同時に報告し、データ行の列検証と job_id 重複検査は行わずに終了コード 2 で終える"
    target: "facade/src/domain/job_map.sh:173"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-020"
    evidence: "重複列名ごとの拒否は明示。欠落列との併記・初出順・後続検証の範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-050"
    tier: "tier-facade"
    assumption: "入力の守備範囲の違反(NUL / 不正な UTF-8 / BOM / CR)が 1 行でもあるときは、その error 行(+ hint)だけを全件報告し、ヘッダー検証・行の検証・job_id 重複検査・版の集計は行わずに終了コード 2 で終える(違反の無い行の csv quote is invalid や列の違反も報告しない)"
    target: "facade/src/usecase/validate_job_map.sh:145"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-021"
    evidence: "config_input_rules と BDD は原因別の報告と二次診断抑止を定義するが、別の正常形式行の値検証を打ち切る範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-051"
    tier: "tier-facade"
    assumption: "同じ行に入力の守備範囲の違反が複数あるとき(例: NUL と CR を含む行)は原因ごとに error 行を出し、1 行の中では byte order mark → nul byte → encoding is not utf-8 → carriage return の順に並べる"
    target: "facade/src/usecase/validate_job_map.sh:47"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-022"
    evidence: "同種違反の行ごとの報告と CR hint 位置は明示。同一行の複数原因の順序は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-052"
    tier: "tier-facade"
    assumption: "mktemp がテンプレート末尾以外の X を置き換えない環境(BSD 系の開発機)では、テンプレートのままの名前で作られた作業ファイルを relay-gate-<pid>-<乱数>.part へ移してから使う(最終名は同じ規則で .part を外した名前。実行環境の Linux ではテンプレート relay-gate-XXXXXX.part をそのまま使う)"
    target: "facade/src/repository/job_map_repo.sh:333"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-023"
    evidence: "接頭辞・.part・rename・0600 は契約に明示。BSD 開発機向けの PID と乱数による代替命名は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-053"
    tier: "tier-facade"
    assumption: "事前走査のパイプライン(tr | sed)の commands= は、sed が非 0 で終わり tr が SIGPIPE(終了状態 141)で終わったときは sed だけとし、sed の出力が想定外(原因の文字と数字以外・証跡なし)のときは sed、走査した総行数と読み込んだ行数が合わないときは tr,sed とする"
    target: "facade/src/repository/job_map_repo.sh:500"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-024"
    evidence: "実際に失敗したコマンド名という契約はあるが、二次的 SIGPIPE や走査結果不整合の帰属判定方式は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-054"
    tier: "tier-facade"
    assumption: "warn: unknown column は未知の列名の出現ごとに 1 行出す(同じ未知の列名が 2 回あれば warn が 2 行と duplicate column の error が 1 行出る)"
    target: "facade/src/domain/job_map.sh:166"
    category: "data_format"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-025"
    evidence: "未知列警告と重複名ごとの error は契約に明示。同名未知列の warn を出現回数分出す選択は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-055"
    tier: "tier-facade"
    assumption: "読み込み後の複製の削除(rm)が失敗したときは、読み込み結果を検証結果として使わず、補助コマンドの失敗として error: internal command failed commands=rm path: <path> の 1 行と終了コード 6 で終える(stdout の map_path / rows / map_version は出さない)"
    target: "facade/src/repository/job_map_repo.sh:307"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "consistent"
    finding_id: "F-026"
    evidence: "config_input_rules.snapshot は読み込み後の削除を必須とし、internal_failure は複製後に使う補助コマンドの失敗を exit 6・stdout なし・契約 error のみにする。rm もこの規則に含まれる。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-056"
    tier: "tier-facade"
    assumption: "複製の工程(mktemp / cat / mv)の失敗と複製の削除(rm)の失敗が重なったときは、入力の複製の失敗(error: config snapshot failed + hint: check TMPDIR is writable、終了コード 6)として報告し、commands=rm の行は出さない"
    target: "facade/src/repository/job_map_repo.sh:307"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-027"
    evidence: "複製工程と削除の同時失敗時に snapshot failed を優先する規則は契約に明示されない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "A-057"
    tier: "tier-facade"
    assumption: "複製完了後の補助コマンド(tr / sed)の失敗と複製の削除(rm)の失敗が重なったときは、commands= に失敗した順でカンマ区切りに並べる(例: commands=sed,rm)"
    target: "facade/src/repository/job_map_repo.sh:312"
    category: "data_format"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-028"
    evidence: "commands に実際の失敗名を列挙する規則はあるが、複数失敗を発生順にする順序は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
assumption_verdicts_summary:
  consistent: 1
  spec_absent: 23
  contradicts: 0
  unlisted: 0
findings:
  - id: "F-003"
    viewpoint: "spec_conformance"
    severity: "major"
    target: "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
    claim: "重複 job_id の lines の列挙対象が契約本文と BDD で矛盾する。前回 F-003 が未解消。"
    evidence: "本文は2回目以降の行番号を指定する一方、tier md BDD は初出を含む lines=2,5 を要求。feature の転写一致を確認し、独立実行でも2,3行目の J1 は exit 2、lines=2,3。入力ソース間矛盾のため実装 blocker としない。"
    suggested_fix: "仕様本文と BDD で初出行の包含を統一する。"
  - id: "F-004"
    viewpoint: "fault_tolerance"
    kind: "spec-gap"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:334"
    claim: "BSD 開発機向けの作業名変更で mv が失敗すると元の relay-gate-XXXXXX.part が残る。"
    evidence: "macOS の実 mktemp と PATH 先頭の mv 失敗 fixture を使用。有効5列1行 CSV に対して exit 6、stdout 空、snapshot failed と hint の2行だが TMPDIR に作業名1件が残存。JOB_MAP_REPO_SNAPSHOT を新名へ更新してから mv するため旧作業名が削除対象から外れる。本番前提は CLAUDE.md のオンプレ Linux。BSD 対応の保証範囲は未規定で Linux の違反は立証していないため minor。fixture と残存物は検証後に削除済み。"
    suggested_fix: "BSD 対応を維持する場合、最初の mv 成功までは元の作業名を削除対象として保持する。"
  - id: "F-005"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-005"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:151"
    claim: "仕様に明示のない判断。ヘッダー行がクォート不正のときは、ヘッダー行とデータ行の csv quote is invalid だけを全件報告し、ヘッダー検証と列検証を行わずに終了コード 2 で終える"
    evidence: "tier md UC ロジックはクォート不正を拒否するが、解析不能ヘッダー後のデータ行検証の範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-006"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-006"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:158"
    claim: "仕様に明示のない判断。ヘッダー不一致(必須列の欠落、host / user の片方だけ)のときは、データ行の列検証と job_id 重複検査を行わず、データ行の csv quote is invalid だけを追加で報告して終了コード 2 で終える"
    evidence: "必須列と host/user 対の拒否は明記。列対応不能時にクォート検証だけ続ける範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-007"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-008"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:150"
    claim: "仕様に明示のない判断。job map header mismatch の missing= に複数の列を並べるときは、契約の列順(job_id, host, user, work_dir, script, fixed_params, hang_detect_limit_minutes)で並べる"
    evidence: "契約の missing=<col,...> は複数欠落列の並び順を定めていない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-008"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-010"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:176"
    claim: "仕様に明示のない判断。csv quote is invalid または column count mismatch になったデータ行は、列ごとの検証、job_id 重複検査、map_version の集計の対象にしない"
    evidence: "クォート・列数の拒否は明記されるが、重複検査と版集計の母集団への包含は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-009"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-012"
    severity: "major"
    target: "facade/src/domain/job_map.sh:281"
    claim: "仕様に明示のない判断。credential_ref が参照名の形(英数字と _ . - だけ)に合わない値は、/ も BEGIN も含まなくても warn: credential_ref looks like a secret or path を出し、拒否はしない"
    evidence: "credential_ref の正規表現と /・BEGIN の warn は明示。その他の形式不一致を警告にするか拒否するかは明示されていない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-010"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-014"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:420"
    claim: "仕様に明示のない判断。info: resolved 行の fixed_params= は、解析後の要素を要素間に空白を置かない 1 行の JSON 配列に組み立て直し、スラッシュ(/)と U+00A0 以上の非 ASCII 文字はエスケープしない"
    evidence: "control_chars の JSON 表記は規定されるが、等価な JSON の要素間空白と任意エスケープの正規形は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-011"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-016"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:509"
    claim: "仕様に明示のない判断。hang_detect_limit_minutes が導入時の推奨値 60 と違う行に対する専用の info: 行は出さず、--verbose の info: resolved 行の hang_detect_limit_minutes=N で値を示すだけにする"
    evidence: "spec.md の推奨値との差を info とする記載に専用文言はなく、CLI 契約には verbose の resolved 行がある。専用行を省く選択は明示されない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-012"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-017"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:655"
    claim: "仕様に明示のない判断。map_version の distinct 値が複数あるとき、stdout の map_version= と warn: mixed map_version の values= には、distinct 値を初出順のカンマ区切りで出す"
    evidence: "spec.md 計算ルールは全行の distinct 値と混在警告を定義するが、初出順は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-013"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-018"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:655"
    claim: "仕様に明示のない判断。map_version が空の行と値のある行が混在するとき、空の値は warn: mixed map_version の values= と stdout の map_version= に空値表記 - で出す(例: 空の行と v1 の行なら values=-,v1 と map_version=-,v1)"
    evidence: "空値単体の - は ui-design にあるが、混在版リスト内の空要素への適用は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-014"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-019"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:180"
    claim: "仕様に明示のない判断。--verbose の info: resolved 行は列ごとの検証を通過した行にだけ出し、違反のある行には出さない(他の行に違反があって終了コード 2 になるときも、通過した行の分は出す)"
    evidence: "契約 stderr は verbose 解決結果の形式を定義するが、違反行との混在時の出力範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-015"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-020"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:129"
    claim: "仕様に明示のない判断。stderr の行は、入力の守備範囲の error(行番号順。hint は最後の CR の次)、ヘッダーの unknown column の warn、duplicate column の error、header mismatch の error、データ行ごとの error と warn と info(行番号の昇順)、duplicate job_id、mixed map_version の順に、検証を終えてからまとめて出す"
    evidence: "全件報告と CR hint の位置以外に、異種の error/warn/info 間の全体順序は規定されない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-016"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-022"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:189"
    claim: "仕様に明示のない判断。job_id 重複検査は、job_id is invalid になった値も文字列(バイト列の完全一致)として比較の対象に含め、空の job_id だけを対象から外す"
    evidence: "job_id の形式と一意性は明示。不正値・空値を重複比較へ含める範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-017"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-025"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:636"
    claim: "仕様に明示のない判断。複数の job_id が重複しているとき、error: duplicate job_id の行は、その job_id が 1 回目に現れた行番号の昇順で出す"
    evidence: "複数 job_id の重複報告順は未規定。初出行を lines に含める別の本文/BDD矛盾は独立 finding とする。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-018"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-043"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:183"
    claim: "仕様に明示のない判断。検証を通過した行の解決結果(info: resolved)や版の集計を作れない内部障害は、補助コマンドの失敗と同じ error: internal command failed commands=<関数名> path: <path> の 1 行と終了コード 6 で終える(commands= に外部コマンドではなく関数名 job_map_row_resolved_line / job_map_version_summary を入れる)"
    evidence: "internal_failure は補助コマンドの失敗を規定するが、内部関数名を commands に載せる拡張は未規定。関数差替え後の正しさは対象外。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-019"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-044"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:605"
    claim: "仕様に明示のない判断。補助コマンドが終了状態 0 を返しても、次のどれかに当たるときは補助コマンドの失敗として扱う。sort の出力の行数が入力の行数と違う。sort の出力が job_id のバイト列の昇順に並んでいない。事前走査の出力に最終行まで走査した証跡(証跡の文字と総行数)が無い・想定外の文字がある。事前走査が数えた総行数と bash が読み込んだ行数が違う"
    evidence: "想定外出力の拒否は internal_failure に明示。行数・並び順・終端証跡という具体的検査方式は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-020"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-049"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:173"
    claim: "仕様に明示のない判断。ヘッダー列名の重複があるときは、duplicate column の error(重複した列名ごとに 1 行、初出順)に加えて必須列の欠落があれば header mismatch の error も同時に報告し、データ行の列検証と job_id 重複検査は行わずに終了コード 2 で終える"
    evidence: "重複列名ごとの拒否は明示。欠落列との併記・初出順・後続検証の範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-021"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-050"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:145"
    claim: "仕様に明示のない判断。入力の守備範囲の違反(NUL / 不正な UTF-8 / BOM / CR)が 1 行でもあるときは、その error 行(+ hint)だけを全件報告し、ヘッダー検証・行の検証・job_id 重複検査・版の集計は行わずに終了コード 2 で終える(違反の無い行の csv quote is invalid や列の違反も報告しない)"
    evidence: "config_input_rules と BDD は原因別の報告と二次診断抑止を定義するが、別の正常形式行の値検証を打ち切る範囲は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-022"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-051"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:47"
    claim: "仕様に明示のない判断。同じ行に入力の守備範囲の違反が複数あるとき(例: NUL と CR を含む行)は原因ごとに error 行を出し、1 行の中では byte order mark → nul byte → encoding is not utf-8 → carriage return の順に並べる"
    evidence: "同種違反の行ごとの報告と CR hint 位置は明示。同一行の複数原因の順序は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-023"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-052"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:333"
    claim: "仕様に明示のない判断。mktemp がテンプレート末尾以外の X を置き換えない環境(BSD 系の開発機)では、テンプレートのままの名前で作られた作業ファイルを relay-gate-<pid>-<乱数>.part へ移してから使う(最終名は同じ規則で .part を外した名前。実行環境の Linux ではテンプレート relay-gate-XXXXXX.part をそのまま使う)"
    evidence: "接頭辞・.part・rename・0600 は契約に明示。BSD 開発機向けの PID と乱数による代替命名は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-024"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-053"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:500"
    claim: "仕様に明示のない判断。事前走査のパイプライン(tr | sed)の commands= は、sed が非 0 で終わり tr が SIGPIPE(終了状態 141)で終わったときは sed だけとし、sed の出力が想定外(原因の文字と数字以外・証跡なし)のときは sed、走査した総行数と読み込んだ行数が合わないときは tr,sed とする"
    evidence: "実際に失敗したコマンド名という契約はあるが、二次的 SIGPIPE や走査結果不整合の帰属判定方式は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-025"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-054"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:166"
    claim: "仕様に明示のない判断。warn: unknown column は未知の列名の出現ごとに 1 行出す(同じ未知の列名が 2 回あれば warn が 2 行と duplicate column の error が 1 行出る)"
    evidence: "未知列警告と重複名ごとの error は契約に明示。同名未知列の warn を出現回数分出す選択は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-026"
    viewpoint: "assumption_conformance"
    kind: "restatement"
    assumption_id: "A-055"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:307"
    claim: "仕様の復唱。読み込み後の複製の削除(rm)が失敗したときは、読み込み結果を検証結果として使わず、補助コマンドの失敗として error: internal command failed commands=rm path: <path> の 1 行と終了コード 6 で終える(stdout の map_path / rows / map_version は出さない)"
    evidence: "config_input_rules.snapshot は読み込み後の削除を必須とし、internal_failure は複製後に使う補助コマンドの失敗を exit 6・stdout なし・契約 error のみにする。rm もこの規則に含まれる。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-027"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-056"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:307"
    claim: "仕様に明示のない判断。複製の工程(mktemp / cat / mv)の失敗と複製の削除(rm)の失敗が重なったときは、入力の複製の失敗(error: config snapshot failed + hint: check TMPDIR is writable、終了コード 6)として報告し、commands=rm の行は出さない"
    evidence: "複製工程と削除の同時失敗時に snapshot failed を優先する規則は契約に明示されない。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-028"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-057"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:312"
    claim: "仕様に明示のない判断。複製完了後の補助コマンド(tr / sed)の失敗と複製の削除(rm)の失敗が重なったときは、commands= に失敗した順でカンマ区切りに並べる(例: commands=sed,rm)"
    evidence: "commands に実際の失敗名を列挙する規則はあるが、複数失敗を発生順にする順序は未規定。 S4 固定指示にこの具体的挙動の追加指定はない。実装の該当関数と契約を照合し、reason/confidence/spec_refs は判定確定後に読んだ。"
  - id: "F-029"
    viewpoint: "assumption_conformance"
    kind: "category_mismatch"
    assumption_id: "A-010"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:176"
    claim: "分類の独立判定が異なる。記録は error_handling、Verifier は input_validation。"
    evidence: "入力行の検証対象選択は input_validation、失敗・違反がある場合の診断や結果の出し方は error_handling として分類した。verdict と severity は分類差で変更しない。"
  - id: "F-030"
    viewpoint: "assumption_conformance"
    kind: "category_mismatch"
    assumption_id: "A-019"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:180"
    claim: "分類の独立判定が異なる。記録は data_format、Verifier は error_handling。"
    evidence: "入力行の検証対象選択は input_validation、失敗・違反がある場合の診断や結果の出し方は error_handling として分類した。verdict と severity は分類差で変更しない。"
  - id: "F-031"
    viewpoint: "assumption_conformance"
    kind: "category_mismatch"
    assumption_id: "A-054"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:166"
    claim: "分類の独立判定が異なる。記録は data_format、Verifier は error_handling。"
    evidence: "入力行の検証対象選択は input_validation、失敗・違反がある場合の診断や結果の出し方は error_handling として分類した。verdict と severity は分類差で変更しない。"
  - id: "F-032"
    viewpoint: "assumption_conformance"
    kind: "category_mismatch"
    assumption_id: "A-057"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:312"
    claim: "分類の独立判定が異なる。記録は data_format、Verifier は error_handling。"
    evidence: "入力行の検証対象選択は input_validation、失敗・違反がある場合の診断や結果の出し方は error_handling として分類した。verdict と severity は分類差で変更しない。"
summary:
  blocker: 0
  major: 2
  minor: 28
resolved_findings:
  - id: "F-001"
    previous_attempt: 5
    status: "resolved"
    previous_severity: "blocker"
    evidence: "PATH先頭のtr/sed/sort実プロセスが生stderrを出してexit 1する各条件で、exit 6、stdout空、stderrは契約errorの1行だけ。生診断は漏れない。"
  - id: "F-002"
    previous_attempt: 5
    status: "resolved"
    previous_severity: "blocker"
    evidence: "有効CSVでPATH先頭のrmが生stderrを出してexit 1すると、exit 6、stdout空、stderrはcommands=rmの契約error1行。削除不能時に成功集計を返さない。残存ファイルはテスト後にPythonで削除。"
performance_measurement:
  rows: 5000
  bytes: 535659
  all_columns_nonempty: true
  job_ids_unique: true
  map_versions_unique: true
  normal_seconds: 3.8802244998514652
  verbose_seconds: 4.768128083087504
  limit_seconds: 10
  result: "pass"
  recipe: "Python csv.writer LF。i=0..4999。JOB+i, host-+i, batch, /var/app/work, /opt/app/bin/job.sh, [\"--mode\",\"full\"], 60, ssh-key-+i, map-v+i。subprocessの起動から終了までperf_counterで測定。verboseは5000 info行。"
  environment: "macOS arm64; bash 5.3.15; gates完了後の単独測定。"
environment_failures: []
verification_evidence:
  tdd_tests: 324
  tier_bdd_scenarios: 38
  tier_bdd_steps: 129
  all_gate_exit_codes: 0
  input_probes: "NULコメント、過長UTF8、サロゲート、U+10FFFF超、BOM、CRLF、空白行、空/コメントのみ、ヘッダーのみ、最終LFなし、重複列、重複job_id、20万文字セルを実行。全期待結果一致。"
  policy_examples: "tmp/RelayGateの利用イメージ.md のWindows/相対パスCSVブロックを抽出して実行。空白/カンマ固定引数も別途検査。"
limitations:
  - "gitコマンドはユーザーの禁止に従い未実行。cwdとCLAUDE.mdで対象を確認。"
  - "実行はmacOS/bash5.3.15。Linux/bash5.0最小版での実測は未実施。"
  - "UC BDD/ATDDはS6/S7担当のため今回の4ゲートに含めない。"
assumption_verdicts_sha256: "5e2cbb47dadcc37087c5758482956f2732a18cffcd6565f73932523d4b66c6e6"
validation:
  yaml_parse: "pass"
  raw_assumptions_hash: "pass"
  verdict_structure: "pass"
  note: "標準validateVerdictsのrecord hash照合だけをメモリ上でcanonicalに適合させ、保存値はユーザー指定の実バイトSHA-256を独立照合。"
