schema_version: "1.0"
uc_id: "eff24f55"
tier: "tier-facade"
attempt: 5
extraction:
  candidate_count: 65
  excluded_as_explicit: 27
  recorded_count: 38
assumptions:
  - id: "A-001"
    category: "data_format"
    assumption: "メッセージの line=N は、コメント行と空行も数えたファイル先頭を 1 とする物理行番号にする"
    target: "facade/src/repository/job_map_repo.sh:240"
    reason: "仕様の例はコメント行も空行も無いファイルだけで、コメント行や空行を挟んだときに行番号をどう数えるかの定義が無い"
    confidence: "high"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-004"
    category: "input_validation"
    assumption: "CSV セルの前後の空白は取り除かず、セルの値の一部として扱う"
    target: "facade/src/repository/job_map_repo.sh:78"
    reason: "CSV セルのクォート解析規則に、セル前後の空白を取り除くかどうかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
  - id: "A-005"
    category: "error_handling"
    assumption: "ヘッダー行がクォート不正のときは、ヘッダー行とデータ行の csv quote is invalid だけを全件報告し、ヘッダー検証と列検証を行わずに終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:64"
    reason: "仕様のクォート不正の Scenario はデータ行だけで、ヘッダー行がクォート不正のときに後続の検証をどこまで行うかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#処理フロー"
  - id: "A-006"
    category: "error_handling"
    assumption: "ヘッダー不一致(必須列の欠落、host / user の片方だけ)のときは、データ行の列検証と job_id 重複検査を行わず、データ行の csv quote is invalid だけを追加で報告して終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:70"
    reason: "仕様は全行を検査して全件報告すると定めるが、ヘッダー不一致で列を対応付けられないときにデータ行の検証をどこまで行うかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
  - id: "A-008"
    category: "data_format"
    assumption: "job map header mismatch の missing= に複数の列を並べるときは、契約の列順(job_id, host, user, work_dir, script, fixed_params, hang_detect_limit_minutes)で並べる"
    target: "facade/src/domain/job_map.sh:114"
    reason: "missing=<col,...> がカンマ区切りであることは定義されているが、複数列の並び順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].column_matching"
  - id: "A-009"
    category: "input_validation"
    assumption: "ヘッダーに同じ列名が複数あるときは最初の列を値の対応付けに使い、error も warn も出さない"
    target: "facade/src/domain/job_map.sh:158"
    reason: "ヘッダーの列名が重複したときの扱いとメッセージの定義が無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].column_matching"
  - id: "A-010"
    category: "error_handling"
    assumption: "csv quote is invalid または column count mismatch になったデータ行は、列ごとの検証、job_id 重複検査、map_version の集計の対象にしない"
    target: "facade/src/usecase/validate_job_map.sh:84"
    reason: "列を対応付けられない行を、後続の列検証、重複検査、版の集計に含めるかどうかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
  - id: "A-012"
    category: "security"
    assumption: "credential_ref が参照名の形(英数字と _ . - だけ)に合わない値は、/ も BEGIN も含まなくても warn: credential_ref looks like a secret or path を出し、拒否はしない"
    target: "facade/src/domain/job_map.sh:221"
    reason: "検証表は参照名の正規表現と、/ を含む値と BEGIN を含む値が warn であることを定めるが、正規表現に合わずどちらも含まない値(空白や記号を含む値)の扱いの定義が無い。契約の stderr に credential_ref の error: 行の定義も無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#分岐条件一覧"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-014"
    category: "data_format"
    assumption: "info: resolved 行の fixed_params= は、解析後の要素を空白なしの 1 行の JSON 配列に組み立て直す。要素内のバックスラッシュ、二重引用符、改行、タブは 2 文字のエスケープにし、それ以外の制御文字(U+0001〜U+001F、U+007F、U+0080〜U+009F)は \\u00XX(16 進小文字 4 桁)にする。スラッシュと、U+00A0 以上の非 ASCII 文字はエスケープしない"
    target: "facade/src/domain/job_map.sh:320"
    reason: "契約は fixed_params を解析後の配列の JSON 配列表記で出すと定めるが、組み立て直すときのエスケープの選び方(JSON として等価な複数の表記のどれにするか)と空白の有無の定義が無い。U+007F と C1 制御文字は JSON ではエスケープ不要だが、端末が制御文字として解釈するため、任意入力の表示の表記(A-040)と同じ関数でエスケープする"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-016"
    category: "data_format"
    assumption: "hang_detect_limit_minutes が導入時の推奨値 60 と違う行に対する専用の info: 行は出さず、--verbose の info: resolved 行の hang_detect_limit_minutes=N で値を示すだけにする"
    target: "facade/src/domain/job_map.sh:412"
    reason: "spec.md のバリエーション一覧は推奨値との差を info: と記すが、その info: 行の文言と出力条件が tier-facade.md の出力契約と CLI コマンド契約のどちらにも無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#バリエーション一覧"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-017"
    category: "data_format"
    assumption: "map_version の distinct 値が複数あるとき、stdout の map_version= と warn: mixed map_version の values= には、distinct 値を初出順のカンマ区切りで出す"
    target: "facade/src/domain/job_map.sh:553"
    reason: "版の混在は warn で終了コード 0 と定義されているが、そのときの stdout の map_version の値と distinct 値の並び順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#計算ルール一覧"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-018"
    category: "data_format"
    assumption: "map_version が空の行と値のある行が混在するとき、空の値は warn: mixed map_version の values= と stdout の map_version= に空値表記 - で出す(例: 空の行と v1 の行なら values=-,v1 と map_version=-,v1)"
    target: "facade/src/domain/job_map.sh:553"
    reason: "計算ルール「版の集計」は全行の distinct 値を数え複数あれば warn と定めるため、空も distinct 値の 1 つとして数える。ただし distinct 値の一覧の中で空の値をどう表記するかの定義が無い(ui-design.md の空値表記 - は key=value の値全体が空のときの規則)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#計算ルール一覧"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/_cross-cutting/ux-ui/ui-design.md#出力フォーマット"
  - id: "A-019"
    category: "data_format"
    assumption: "--verbose の info: resolved 行は列ごとの検証を通過した行にだけ出し、違反のある行には出さない(他の行に違反があって終了コード 2 になるときも、通過した行の分は出す)"
    target: "facade/src/usecase/validate_job_map.sh:93"
    reason: "契約は info: resolved を 1 行 1 job_id で出し終了コードに影響しないと定めるが、違反のある行や検証 NG のときに出すかどうかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-020"
    category: "data_format"
    assumption: "stderr の行は、ヘッダーの unknown column の warn、データ行ごとの error と warn と info(行番号の昇順)、duplicate job_id、mixed map_version の順に出す"
    target: "facade/src/usecase/validate_job_map.sh:42"
    reason: "stdout の固定順は定義されているが、stderr に出す複数種類の行の出力順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-022"
    category: "input_validation"
    assumption: "job_id 重複検査は、job_id is invalid になった値も文字列(バイト列の完全一致)として比較の対象に含め、空の job_id だけを対象から外す"
    target: "facade/src/usecase/validate_job_map.sh:100"
    reason: "job_id の形式違反や空の行を重複検査に含めるかどうかの定義が無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#分岐条件一覧"
  - id: "A-024"
    category: "performance"
    assumption: "job_id 重複検査は全組合せの比較をせず、行番号と job_id の組を外部コマンド sort(LC_ALL=C の安定ソート)で job_id のバイト列順に並べ、隣り合う同じ値をまとめて検出する。bash の連想配列は使わない"
    target: "facade/src/domain/job_map.sh:497"
    reason: "tier-facade.md は job_id の一意性を要求するが探索方式を指定しない。対象 bash の版と最大マップ行数の定義も無いため、bash の版に依存せず行数にほぼ比例する時間で終わる方式を選んだ(attempt 1 の独立検証 F-002 / F-031 の指摘。実測は 2000 行で約 1.1 秒、5000 行で約 2.6 秒)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/dev-rules/coding-rules.md"
  - id: "A-025"
    category: "data_format"
    assumption: "複数の job_id が重複しているとき、error: duplicate job_id の行は、その job_id が 1 回目に現れた行番号の昇順で出す"
    target: "facade/src/domain/job_map.sh:536"
    reason: "契約は重複した job_id ごとの行の形式と lines= の列挙を定めるが、重複した job_id が複数あるときの error: 行どうしの出力順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-026"
    category: "error_handling"
    assumption: "NUL バイト(0x00)を含む行は、ヘッダー行でもデータ行でも、NUL を取り除いた文字列を検証せずに error: csv quote is invalid line=N path: <path> で報告し、終了コード 2 で終える"
    target: "facade/src/repository/job_map_repo.sh:351"
    reason: "bash の read は NUL を黙って落とすため、落ちた後の文字列を検証すると不正な値を受理する(attempt 2 の独立検証 F-001)。仕様と契約に NUL や文字として扱えないバイトを含む行の error: 行の定義が無い。S4 の dispatch 指示は、契約に無い文言を作らず最も近い既存の設定検証エラーで拒否することを求めた。csv_rules は bash 単独で 1 文字ずつ解析できる範囲に限定し、その違反を csv quote is invalid と定めるため、行番号を持つこの行を選んだ(attempt 3 の独立検証 F-031 に従い、既存の診断への対応付けの判断として error_handling に分類した)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-028"
    category: "input_validation"
    assumption: "NUL バイトと不正な UTF-8 の検出は、bash が行を読み込む前に、入力の複製(A-045)を外部コマンド tr と sed(どちらも LC_ALL=C。基本正規表現と生のバイトだけを使う)で 1 回走査し、該当する行の行番号だけを取り出す方式にする。bash の行の読み込みも同じ複製から行い、元のパスは走査にも読み込みにも使わない"
    target: "facade/src/repository/job_map_repo.sh:288"
    reason: "bash の文字列は NUL を保持できず、bash 単独では NUL の有無を判定できない。仕様は CSV セルの解析を外部 CSV パーサに依存しないと定めるが、解析の前段でバイトを検査する手段の定義が無い。tr と sed は POSIX の基本コマンドで、エアーギャップ環境の Linux に追加の導入物なしで存在する。S4 の dispatch 指示は、標準コマンドだけを使いロケールに依存させないことを求めた。attempt 4 は走査と読み込みで元のパスを別々に開いていたため、間に同じ行数のファイルへ mv で差し替えると NUL を含む行を受理した(attempt 4 の独立検証 F-001)。attempt 5 の dispatch 指示(利用者の決定)に従い、走査と読み込みを同じ複製に揃えた"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
      - "docs/dev-rules/coding-rules.md"
  - id: "A-029"
    category: "input_validation"
    assumption: "CR(0x0d)は改行として扱わず、セルの値の一部として扱う(CRLF 改行のファイルは行末のセルと最後の列名に CR が残り、専用の error も warn も出さない)"
    target: "facade/src/repository/job_map_repo.sh:248"
    reason: "仕様は改行コードを LF と定めるが、CRLF 改行や CR だけの改行のファイルを渡されたときの扱いとメッセージの定義が無い。CR を黙って取り除くと仕様に無い正規化を足すことになるため、値を変えずに既存の検証(ヘッダー検証、未知列の warn、列ごとの値検証、列数検証)の結果に任せた"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].format"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-030"
    category: "input_validation"
    assumption: "ファイル先頭の UTF-8 の BOM(0xEF 0xBB 0xBF)は取り除かず、1 行目の先頭のセルの値の一部として扱う(専用の error も warn も出さない)"
    target: "facade/src/repository/job_map_repo.sh:240"
    reason: "仕様は文字コードを UTF-8 と定めるが、BOM の有無と BOM があるときの扱いの定義が無い。BOM 付きのファイルは、1 行目がヘッダー行なら先頭の列名が一致せず unknown column の warn と必須列の欠落の検証に従い、1 行目が # のコメント行ならその行は行頭が # でないためヘッダー行として扱われる"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].format"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-031"
    category: "error_handling"
    assumption: "UTF-8 として不正なバイト列を含む行は、ヘッダー行でもデータ行でも、セルに分解せず検証もせずに error: csv quote is invalid line=N path: <path> で報告し、終了コード 2 で終える(NUL バイトを含む行と同じ扱い)"
    target: "facade/src/repository/job_map_repo.sh:351"
    reason: "設定契約は文字コードを UTF-8 と定めるが、不正なバイト列を含む行の error: 行の定義が無い(attempt 3 の独立検証 F-025 は受理を仕様との矛盾と判定した)。S4 の dispatch 指示(利用者の決定 2026-09-19)は、仕様に無い壊れた入力を、契約に無い文言を作らず最も近い既存の設定検証エラーと終了コードで拒否することを求めた。行番号を持ち、CSV として解析できない行を表す csv quote is invalid を選んだ"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-033"
    category: "performance"
    assumption: "CSV 行の解析と fixed_params の JSON 解析・再エスケープは、1 文字ずつ添字で取り出さず、値を 1024 バイトの窓に区切り、区切り文字以外の連続した文字をまとめて取り込む。受理する文法と解析結果は 1 文字ずつの状態機械と同じにする"
    target: "facade/src/repository/job_map_repo.sh:68"
    reason: "csv_rules の「1 文字ずつ状態機械で解析できる範囲に限定する」は受理する文法の範囲の定めで、走査の実装方式の定義は無い。bash の文字列操作は文字列全体の長さに比例する時間がかかり、1 文字ずつの走査は長さの 2 乗の時間になる(attempt 2 の実装は 10 万文字の行で約 100 秒)。NFR B.2.1.1 の CLI 応答 10 秒目標に合わせた(実測は 10 万文字の行で 0.2 秒、二重化引用符やエスケープが密な 10 万文字の行で 8 秒未満)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/nfr/latest/nfr-grade.yaml#B.2.1.1"
  - id: "A-034"
    category: "input_validation"
    assumption: "行の長さとセルの値の長さに上限を設けず、長さを理由に拒否も警告もしない"
    target: "facade/src/repository/job_map_repo.sh:78"
    reason: "仕様と契約に、行やセルの最大長とそれを超えたときのメッセージの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-035"
    category: "input_validation"
    assumption: "半角空白やタブだけの行は空行として扱わず、1 セルの行として扱う(ヘッダー行より後ろなら列数検証の対象のデータ行になる)"
    target: "facade/src/repository/job_map_repo.sh:258"
    reason: "仕様は空行を無視すると定めるが、空白文字だけの行を空行に含めるかどうかの定義が無い。セル前後の空白を取り除かない前提(A-004)と同じ扱いにした"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].format"
  - id: "A-036"
    category: "input_validation"
    assumption: "最終行の末尾に改行(LF)が無くても、その行を他の行と同じようにヘッダー行またはデータ行として読む"
    target: "facade/src/repository/job_map_repo.sh:248"
    reason: "仕様は改行コードを LF と定めるが、最終行の末尾の改行が無いファイルの扱いの定義が無い。bash の read は改行の無い最終行を失敗として返すため、読み飛ばすと最終行の違反を検出できない"
    confidence: "high"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].format"
  - id: "A-037"
    category: "input_validation"
    assumption: "ヘッダー行だけでデータ行が 0 件のファイルは、検証 OK(終了コード 0、rows=0、map_version=-)として受理する"
    target: "facade/src/usecase/validate_job_map.sh:78"
    reason: "設定契約は必須列を定めるが、データ行の最小件数の定義が無い(attempt 3 の独立検証 F-032 が、記録されていない判断として指摘した)。error が 0 件なら終了コード 0 という終了コード表に従った"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#計算ルール一覧"
  - id: "A-038"
    category: "input_validation"
    assumption: "行頭 # のコメント行でも、UTF-8 として不正なバイト列を含む行は無視せず、A-031 と同じ csv quote is invalid line=N で拒否する"
    target: "facade/src/repository/job_map_repo.sh:55"
    reason: "設定契約は行頭 # の行を無視すると定め、文字コードを UTF-8 とも定めるが、コメント行が UTF-8 でないファイルをどちらの規則で扱うかの定義が無い。文字コードはファイル全体の形式と解釈し、UTF-8 でないファイルを検証 OK にしない側を選んだ(NUL バイトは UTF-8 として妥当な U+0000 なので、NUL を含むコメント行は行頭 # の規則どおり無視する)"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].format"
  - id: "A-039"
    category: "input_validation"
    assumption: "UTF-8 として妥当かどうかは RFC 3629 の範囲で判定する(過長形式、サロゲート U+D800〜U+DFFF、U+10FFFF を超える並び、先頭バイトの無い継続バイト、続きの足りない先頭バイトは不正。BOM と非文字 U+FFFE / U+FFFF は妥当として扱う)"
    target: "facade/src/repository/job_map_repo.sh:44"
    reason: "設定契約は文字コードを UTF-8 と定めるが、どの規格の範囲で妥当とするか(過長形式やサロゲートの扱い)の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].format"
  - id: "A-040"
    category: "data_format"
    assumption: "任意入力(セルの値、列名、パス、未知のオプションや余分な引数)を stdout / stderr の行に出すときは、制御文字を可視表記へ置き換える。改行は \\n、タブは \\t、それ以外の U+0001〜U+001F と U+007F と UTF-8 で符号化した U+0080〜U+009F は \\u00XX(16 進小文字 4 桁)にする。バックスラッシュは置き換えない(制御文字を含まない値の出力は変えない)"
    target: "facade/src/domain/cli_field.sh:37"
    reason: "出力フォーマット規約は ANSI エスケープを出さないことと 1 行 1 事実を定めるが、任意入力に制御文字があるときの置き換え表記の定義が無い(attempt 3 の独立検証 F-001)。S4 の dispatch 指示(利用者の決定 2026-09-19)は、安全な可視表記へ置き換え、表記を verbose の fixed_params= の JSON エスケープと整合させることを求めた。バックスラッシュを置き換えないため、生のタブと文字としての \\t は表示上区別できない"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/ux-ui/ui-design.md#出力フォーマット"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.output_format"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-041"
    category: "data_format"
    assumption: "stdout の key=value と key: value の選択は、制御文字を置き換える前の値に空白(タブや改行を含む)があるかで決める(例: タブを含む map_version は map_version: a\\tb)"
    target: "facade/src/domain/cli_field.sh:123"
    reason: "出力フォーマット規約は値に空白を含むとき key: value と定めるが、空白にあたる制御文字を可視表記へ置き換えた値をどちらの形式で出すかの定義が無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/ux-ui/ui-design.md#出力フォーマット"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.output_format.stdout"
  - id: "A-042"
    category: "data_format"
    assumption: "引数のパスやオプションに含まれる UTF-8 として不正なバイト(制御文字でないもの)は、置き換えも拒否もせず、そのままのバイトで出力する"
    target: "facade/src/domain/cli_field.sh:37"
    reason: "conventions.output_format.path_uri はパスをクォートせずそのまま出すと定めるが、パスが UTF-8 として不正なバイトを含むときの表記の定義が無い。設定契約の文字コード UTF-8 はファイルの内容の形式で、引数のパスには及ばないと解釈した"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.output_format.path_uri"
      - "docs/specs/latest/_cross-cutting/ux-ui/ui-design.md#出力フォーマット"
  - id: "A-043"
    category: "error_handling"
    assumption: "検証に使う補助コマンドが失敗したときは、検証 OK にも検証違反にもせず、stdout を出さずに終了コード 6 で終える。stderr は、事前走査の tr / sed の失敗なら error: internal command failed commands=tr,sed path: <path>、job_id 重複検査の sort の失敗なら error: internal command failed commands=sort の 1 行(検証を通過した行の解決結果や版の集計を作れない内部障害も、commands= に関数名を入れた同じ行と終了コード 6)"
    target: "facade/src/usecase/validate_job_map.sh:32"
    reason: "契約の validate-config.sh の終了コード 6 の条件は runner --help 問い合わせの準備失敗だけで、--job-map の検証器の内部障害の条件と error: 行の定義が無い(attempt 3 の独立検証 F-002)。S4 の dispatch 指示は、失敗時に成功サマリーを返さず異常終了にし、終了コードは CLI 契約の既存の区分に従うことを求めた。conventions.exit_codes の 6(実行エラー。内部エラーを含む)を選び、文言は message_grammar の failed を使った"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.exit_codes"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].exit_codes"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-044"
    category: "error_handling"
    assumption: "補助コマンドが終了状態 0 を返しても、次のどれかに当たるときは補助コマンドの失敗(A-043)として扱う。sort の出力の行数が入力の行数と違う。sort の出力が job_id のバイト列の昇順に並んでいない。事前走査の出力に、最終行まで走査した証跡(証跡の行と総行数)が無い・想定外の文字がある。事前走査が数えた総行数と bash が読み込んだ行数が違う"
    target: "facade/src/domain/job_map.sh:501"
    reason: "仕様と契約に、検証器が使う補助コマンドの出力の妥当性をどこまで確かめるかの定義が無い。終了状態だけでは、出力を返さずに 0 で終わる障害を重複なしや異常行なしとして受理してしまう(事前走査は出力なしが正常な結果のため、走査の完了を示す行を sed に出させて確かめる)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].exit_codes"
  - id: "A-045"
    category: "input_validation"
    assumption: "ジョブマップの検証は、検証の開始時に 1 回だけ取った入力ファイルの複製(その時点のバイト列)だけを対象にする。検証中に同じパスのファイルが差し替え・変更されても、差し替え前(複製した時点)の内容だけで検証し、差し替えを検知も報告もしない"
    target: "facade/src/repository/job_map_repo.sh:196"
    reason: "仕様と契約は、検証中に入力ファイルが差し替えられたときにどの内容を検証結果とするかを定めない。CLI 契約の編集規則は一時ファイルを書いて mv で置き換える運用を定めるため、検証と差し替えが重なり得る。attempt 4 は点検と解析で別の内容を読み、どちらのファイルにも無い行を受理した(attempt 4 の独立検証 F-001)。attempt 5 の dispatch 指示(利用者の決定)は、ファイルを 1 回だけ読み、点検と解析が必ず同じ不変の内容を参照することを求めた。差し替えの検知(複製の前後の比較など)は仕様に無い機能を足すことになるため行わない"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].edit_rule"
  - id: "A-046"
    category: "error_handling"
    assumption: "入力の複製に失敗したとき(mktemp で一時ファイルを作れない、cat による複製が失敗した)は、検証 OK にも検証違反にもせず、stdout を出さずに、事前走査の失敗と同じ error: internal command failed commands=tr,sed path: <path> の 1 行と終了コード 6 で終える(commands= に mktemp や cat は出さない)"
    target: "facade/src/usecase/validate_job_map.sh:58"
    reason: "仕様と契約に、検証器が入力を複製できないときの終了コードと error: 行の定義が無い。attempt 5 の dispatch 指示は、複製の失敗も attempt 4 と同じく検知し、終了コード 6 と既存の internal error 行で終えることを求めた。複製は事前走査の前段なので、新しい文言を作らず事前走査の失敗の行に含めた"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.exit_codes"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-047"
    category: "security"
    assumption: "入力の複製は環境変数 TMPDIR のディレクトリ(未設定なら /tmp)に mktemp で relay-gate-job-map.XXXXXX の名前で作り(権限は mktemp の既定の所有者だけが読み書きできる 0600)、読み込みを終えたら成功でも失敗でも削除する。出力の path: と map_path: には複製のパスではなく利用者が指定したパスを出す"
    target: "facade/src/repository/job_map_repo.sh:219"
    reason: "仕様と契約は、検証器が入力の内容を一時的にディスクへ置くことも、その置き場所・権限・削除時期も定めない。複製にはジョブマップの内容(credential_ref の参照名を含む)がそのまま入るため、他の利用者から読めない権限で作り、使い終えたら残さないようにした。置き場所は既存の runner の --help 問い合わせの一時ファイルと同じ TMPDIR の規則に揃えた。dispatch 指示は、元のパスのまま表示し一時ファイルのパスを出力に漏らさないことを求めた"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.output_format.path_uri"
  - id: "A-048"
    category: "error_handling"
    assumption: "入力の読み込み中に HUP / INT / TERM を受けたら、入力の複製を削除してから終了コード 129 / 130 / 143(128 + シグナル番号)で終える。この trap は読み込み中だけ登録し、読み込みを終えたら呼び出し元の trap に戻す。EXIT の trap は登録せず、戻る経路ごとに複製を削除する(SIGKILL で強制終了したときは複製が残り得る)"
    target: "facade/src/repository/job_map_repo.sh:196"
    reason: "仕様と契約に、検証の途中でシグナルを受けたときの終了コードと後始末の定義が無い。attempt 5 の dispatch 指示は、一時ファイルを正常終了・異常終了・シグナルのいずれでも削除することを求めた。bash はサブシェルやコマンド置換の中で、実際には効いていない親の trap を trap -p に表示することがあり(bash 5.3 で確認)、EXIT を表示どおりに戻すとサブシェルの終了時に親の終了処理が動くため、EXIT は差し替えない。読み込みの関数の中の失敗はすべて条件文の中で戻り値として受け、exit に至る経路は無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.exit_codes"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].exit_codes"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
