schema_version: "1.0"
uc_id: "eff24f55"
tier: "tier-facade"
attempt: 5
# 仕様還流(spec event 20260921_100000_feedback_impl_feedback_eff24f55)後の再実行で作り直した。
# 旧 attempt 5 の前提のうち、還流で仕様が確定した事項(NUL / 不正な UTF-8 / BOM / CR の原因ごとの拒否、単一スナップショットの
# 置き場所・名前・権限・削除、複製失敗と補助コマンド失敗の文言と終了コード 6、制御文字の可視表記、work_dir / script の非空のみ検証、
# 行の長さの上限なし、空白だけの行、最終行の改行なし、データ行 0 件、bash 5.0 と tr / sed / sort の許容 など)は除外した。
extraction:
  candidate_count: 44
  excluded_as_explicit: 20
  recorded_count: 24
assumptions:
  - id: "A-001"
    category: "data_format"
    assumption: "クォート不正・列数不一致・列の値の違反・duplicate job_id の line=N は、コメント行と空行も数えたファイル先頭を 1 とする物理行番号にする(入力の守備範囲の error 行と同じ数え方)"
    target: "facade/src/repository/job_map_repo.sh:515"
    reason: "契約は入力の守備範囲の error 行について『行番号は物理行番号』と定めるが、列検証・クォート不正・重複の line=N の数え方(コメント行や空行を挟んだとき)を定めていない"
    confidence: "high"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-004"
    category: "input_validation"
    assumption: "CSV セルの前後の空白は取り除かず、セルの値の一部として扱う"
    target: "facade/src/repository/job_map_repo.sh:237"
    reason: "CSV セルのクォート解析規則に、セル前後の空白を取り除くかどうかの定義が無い(config_input_rules は『形式の内側の値はそのまま受理する』と定めるが、空白が値の一部かどうかは明記しない)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].csv_rules"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.principle"
  - id: "A-005"
    category: "error_handling"
    assumption: "ヘッダー行がクォート不正のときは、ヘッダー行とデータ行の csv quote is invalid だけを全件報告し、ヘッダー検証と列検証を行わずに終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:151"
    reason: "仕様のクォート不正の Scenario はデータ行だけで、ヘッダー行がクォート不正のときに後続の検証をどこまで行うかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#処理フロー"
  - id: "A-006"
    category: "error_handling"
    assumption: "ヘッダー不一致(必須列の欠落、host / user の片方だけ)のときは、データ行の列検証と job_id 重複検査を行わず、データ行の csv quote is invalid だけを追加で報告して終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:158"
    reason: "仕様は全行を検査して全件報告すると定めるが、ヘッダー不一致で列を対応付けられないときにデータ行の検証をどこまで行うかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
  - id: "A-008"
    category: "data_format"
    assumption: "job map header mismatch の missing= に複数の列を並べるときは、契約の列順(job_id, host, user, work_dir, script, fixed_params, hang_detect_limit_minutes)で並べる"
    target: "facade/src/domain/job_map.sh:150"
    reason: "missing=<col,...> がカンマ区切りであることは定義されているが、複数列の並び順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_files[<slot>-job-map.csv].column_matching"
  - id: "A-010"
    category: "error_handling"
    assumption: "csv quote is invalid または column count mismatch になったデータ行は、列ごとの検証、job_id 重複検査、map_version の集計の対象にしない"
    target: "facade/src/usecase/validate_job_map.sh:176"
    reason: "列を対応付けられない行を、後続の列検証、重複検査、版の集計に含めるかどうかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
  - id: "A-012"
    category: "security"
    assumption: "credential_ref が参照名の形(英数字と _ . - だけ)に合わない値は、/ も BEGIN も含まなくても warn: credential_ref looks like a secret or path を出し、拒否はしない"
    target: "facade/src/domain/job_map.sh:281"
    reason: "検証表は参照名の正規表現と、/ を含む値と BEGIN を含む値が warn であることを定めるが、正規表現に合わずどちらも含まない値(空白や記号を含む値)の扱いの定義が無い。契約の stderr に credential_ref の error: 行の定義も無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#分岐条件一覧"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-014"
    category: "data_format"
    assumption: "info: resolved 行の fixed_params= は、解析後の要素を要素間に空白を置かない 1 行の JSON 配列に組み立て直し、スラッシュ(/)と U+00A0 以上の非 ASCII 文字はエスケープしない"
    target: "facade/src/domain/job_map.sh:420"
    reason: "契約は fixed_params を JSON の文字列規則(\" と \\ のエスケープ、制御文字の \\n / \\t / \\u00XX)で出すと定めるが、JSON として等価な複数の表記のうちどれにするか(要素間の空白の有無、/ のエスケープ、非 ASCII 文字の \\uXXXX 化の有無)の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#conventions.output_format.control_chars"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/_cross-cutting/ux-ui/ui-design.md#出力フォーマット"
  - id: "A-016"
    category: "data_format"
    assumption: "hang_detect_limit_minutes が導入時の推奨値 60 と違う行に対する専用の info: 行は出さず、--verbose の info: resolved 行の hang_detect_limit_minutes=N で値を示すだけにする"
    target: "facade/src/domain/job_map.sh:509"
    reason: "spec.md のバリエーション一覧は推奨値との差を info: と記すが、その info: 行の文言と出力条件が tier-facade.md の出力契約と CLI コマンド契約のどちらにも無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#バリエーション一覧"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-017"
    category: "data_format"
    assumption: "map_version の distinct 値が複数あるとき、stdout の map_version= と warn: mixed map_version の values= には、distinct 値を初出順のカンマ区切りで出す"
    target: "facade/src/domain/job_map.sh:654"
    reason: "版の混在は warn で終了コード 0 と定義されているが、そのときの stdout の map_version の値と distinct 値の並び順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#計算ルール一覧"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-018"
    category: "data_format"
    assumption: "map_version が空の行と値のある行が混在するとき、空の値は warn: mixed map_version の values= と stdout の map_version= に空値表記 - で出す(例: 空の行と v1 の行なら values=-,v1 と map_version=-,v1)"
    target: "facade/src/domain/job_map.sh:654"
    reason: "計算ルール「版の集計」は全行の distinct 値を数え複数あれば warn と定めるため、空も distinct 値の 1 つとして数える。ただし distinct 値の一覧の中で空の値をどう表記するかの定義が無い(ui-design.md の空値表記 - は key=value の値全体が空のときの規則)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#計算ルール一覧"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/_cross-cutting/ux-ui/ui-design.md#出力フォーマット"
  - id: "A-019"
    category: "data_format"
    assumption: "--verbose の info: resolved 行は列ごとの検証を通過した行にだけ出し、違反のある行には出さない(他の行に違反があって終了コード 2 になるときも、通過した行の分は出す)"
    target: "facade/src/usecase/validate_job_map.sh:180"
    reason: "契約は info: resolved を 1 行 1 job_id で出し終了コードに影響しないと定めるが、違反のある行や検証 NG のときに出すかどうかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-020"
    category: "data_format"
    assumption: "stderr の行は、入力の守備範囲の error(行番号順。hint は最後の CR の次)、ヘッダーの unknown column の warn、duplicate column の error、header mismatch の error、データ行ごとの error と warn と info(行番号の昇順)、duplicate job_id、mixed map_version の順に、検証を終えてからまとめて出す"
    target: "facade/src/usecase/validate_job_map.sh:129"
    reason: "stdout の固定順と hint の位置は定義されているが、stderr に出す複数種類の行の出力順と、検証の途中で逐次出すかまとめて出すかの定義が無い(内部障害のときに他の行を出さないため、まとめて出す方式を選んだ)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.internal_failure"
  - id: "A-022"
    category: "input_validation"
    assumption: "job_id 重複検査は、job_id is invalid になった値も文字列(バイト列の完全一致)として比較の対象に含め、空の job_id だけを対象から外す"
    target: "facade/src/usecase/validate_job_map.sh:189"
    reason: "job_id の形式違反や空の行を重複検査に含めるかどうかの定義が無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/spec.md#分岐条件一覧"
  - id: "A-025"
    category: "data_format"
    assumption: "複数の job_id が重複しているとき、error: duplicate job_id の行は、その job_id が 1 回目に現れた行番号の昇順で出す"
    target: "facade/src/domain/job_map.sh:635"
    reason: "契約は重複した job_id ごとの行の形式と lines= の列挙を定めるが、重複した job_id が複数あるときの error: 行どうしの出力順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-039"
    category: "input_validation"
    assumption: "UTF-8 の妥当性は RFC 3629 の範囲で判定し、非文字 U+FFFE / U+FFFF と、ファイル先頭以外の位置にある U+FEFF(BOM と同じ符号位置)は妥当な UTF-8 として受理する(先頭の EF BB BF だけを BOM として拒否する)"
    target: "facade/src/repository/job_map_repo.sh:72"
    reason: "契約は不正なバイト列の例(過長形式・サロゲート・U+10FFFF 超・孤立した継続バイト)と先頭の BOM の拒否を定めるが、非文字や先頭以外の U+FEFF を妥当とするかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.rules"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
  - id: "A-043"
    category: "error_handling"
    assumption: "検証を通過した行の解決結果(info: resolved)や版の集計を作れない内部障害は、補助コマンドの失敗と同じ error: internal command failed commands=<関数名> path: <path> の 1 行と終了コード 6 で終える(commands= に外部コマンドではなく関数名 job_map_row_resolved_line / job_map_version_summary を入れる)"
    target: "facade/src/usecase/validate_job_map.sh:183"
    reason: "契約の internal_failure は複製の工程と補助コマンドの失敗の 2 区分を定めるが、外部コマンドを伴わない検証器内部の障害(解決結果を組み立てられない等)の区分と commands= の値の定義が無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.internal_failure"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].exit_codes"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#出力契約"
  - id: "A-044"
    category: "error_handling"
    assumption: "補助コマンドが終了状態 0 を返しても、次のどれかに当たるときは補助コマンドの失敗として扱う。sort の出力の行数が入力の行数と違う。sort の出力が job_id のバイト列の昇順に並んでいない。事前走査の出力に最終行まで走査した証跡(証跡の文字と総行数)が無い・想定外の文字がある。事前走査が数えた総行数と bash が読み込んだ行数が違う"
    target: "facade/src/domain/job_map.sh:604"
    reason: "契約は補助コマンドの『失敗・想定外の出力』を内部障害と定めるが、想定外の出力をどこまで確かめるか(終了状態だけでよいか)の定義が無い。終了状態だけでは、出力を返さずに 0 で終わる障害を重複なしや異常行なしとして受理してしまう"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.internal_failure"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
  - id: "A-049"
    category: "error_handling"
    assumption: "ヘッダー列名の重複があるときは、duplicate column の error(重複した列名ごとに 1 行、初出順)に加えて必須列の欠落があれば header mismatch の error も同時に報告し、データ行の列検証と job_id 重複検査は行わずに終了コード 2 で終える"
    target: "facade/src/domain/job_map.sh:173"
    reason: "契約は duplicate column の文言と終了コード 2 を定めるが、同じヘッダーに必須列の欠落も重なるときに両方を報告するか、列名が重複したヘッダーでデータ行の検証を続けるかの定義が無い(どちらの列を使うか決められないため続けない側を選んだ)"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.rules"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
  - id: "A-050"
    category: "error_handling"
    assumption: "入力の守備範囲の違反(NUL / 不正な UTF-8 / BOM / CR)が 1 行でもあるときは、その error 行(+ hint)だけを全件報告し、ヘッダー検証・行の検証・job_id 重複検査・版の集計は行わずに終了コード 2 で終える(違反の無い行の csv quote is invalid や列の違反も報告しない)"
    target: "facade/src/usecase/validate_job_map.sh:145"
    reason: "UC ロジックは (2) バイト点検 → (3) ヘッダー → (4) 行 → (5) 重複 の順で全行を検査してから全件報告すると定め、BDD は BOM のとき header mismatch を、NUL のとき csv quote is invalid を出さないことを求めるが、バイト点検の違反があるときに違反の無い他の行の検証結果を報告するかどうかの定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#UC ロジック"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.rules"
  - id: "A-051"
    category: "data_format"
    assumption: "同じ行に入力の守備範囲の違反が複数あるとき(例: NUL と CR を含む行)は原因ごとに error 行を出し、1 行の中では byte order mark → nul byte → encoding is not utf-8 → carriage return の順に並べる"
    target: "facade/src/usecase/validate_job_map.sh:47"
    reason: "契約は『同種の行が複数あれば行ごとに 1 行(全件)』と定めるが、1 行に複数の原因が重なるときに全部を出すか 1 つに絞るか、その並び順の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.rules"
  - id: "A-052"
    category: "data_format"
    assumption: "mktemp がテンプレート末尾以外の X を置き換えない環境(BSD 系の開発機)では、テンプレートのままの名前で作られた作業ファイルを relay-gate-<pid>-<乱数>.part へ移してから使う(最終名は同じ規則で .part を外した名前。実行環境の Linux ではテンプレート relay-gate-XXXXXX.part をそのまま使う)"
    target: "facade/src/repository/job_map_repo.sh:322"
    reason: "契約は mktemp のテンプレート例 relay-gate-XXXXXX.part を示すが、末尾以外の X を置き換えない mktemp(BSD 系)での作業名の決め方の定義が無い(実行環境はオンプレ Linux のため契約はそれを前提にしている)"
    confidence: "high"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.snapshot.validate_config"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
  - id: "A-053"
    category: "error_handling"
    assumption: "事前走査のパイプライン(tr | sed)の commands= は、sed が非 0 で終わり tr が SIGPIPE(終了状態 141)で終わったときは sed だけとし、sed の出力が想定外(原因の文字と数字以外・証跡なし)のときは sed、走査した総行数と読み込んだ行数が合わないときは tr,sed とする"
    target: "facade/src/repository/job_map_repo.sh:484"
    reason: "契約は commands= に『実際に失敗したコマンド名だけ』を書くと定めるが、パイプラインで後段の失敗が前段の SIGPIPE を引き起こす場合や、出力の欠落がどちらのコマンドに起因するか判別できない場合の記載規則の定義が無い"
    confidence: "medium"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#config_input_rules.internal_failure"
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
  - id: "A-054"
    category: "data_format"
    assumption: "warn: unknown column は未知の列名の出現ごとに 1 行出す(同じ未知の列名が 2 回あれば warn が 2 行と duplicate column の error が 1 行出る)"
    target: "facade/src/domain/job_map.sh:166"
    reason: "契約は未知列を warn: unknown column column=<name> で報告すると定めるが、同じ未知の列名が複数あるときに列名ごとに 1 行にまとめるか出現ごとに出すかの定義が無い"
    confidence: "low"
    spec_refs:
      - "docs/specs/latest/_cross-cutting/api/cli-command-contract.yaml#commands[validate-config.sh].stderr"
      - "docs/specs/latest/適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/tier-facade.md#設定契約(slot ジョブマップ CSV)"
