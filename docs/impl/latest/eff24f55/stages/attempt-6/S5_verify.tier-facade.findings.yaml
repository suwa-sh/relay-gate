schema_version: "1.0"
uc_id: "eff24f55"
tier: "tier-facade"
attempt: 6
verified_at: "2026-09-23T02:45:40.136766+00:00"
gate_reexec:
  format: "pass"
  lint: "pass"
  tdd: "pass"
  bdd_tier: "pass"
viewpoints_checked:
  spec_conformance:
    status: "done"
    note: "対象 CLI の引数・9列・終了コード・出力・32 BDDシナリオを突合。gherkin の本文が完全一致。旧形式 API summary のため v2 contract slice 照合は非該当。tables=[]、DB操作なし。重複 target は他tier所有で対象外。"
  readability_maintainability:
    status: "done"
    note: "presentation/usecase/domain/repository の境界、日本語コメント、AAA、テスト命名を確認。format/lint通過。"
  security:
    status: "done"
    note: "credential_ref の9種類を独立実走。形式外は同じwarnを1行、受理して値を非出力。固定printf、制御文字可視化、入力をevalしないことを確認。"
  performance:
    status: "done"
    note: "NFR B.1.1.2/B.2.1.1 の全9列・5000行・全job_id一意・全map_version相違の入力を独立実走し3.503秒、終了コード0。10秒以内。無制限長の全入力を網羅保証した測定ではない。"
  operability:
    status: "done"
    note: "診断の物理行番号、パス、stdout/stderr分離、環境変数TMPDIR、利用者指定パスを確認。"
  fault_tolerance:
    status: "done"
    note: "334単体テストと41 BDDシナリオ通過。複製・補助コマンド・rm失敗、差替え、信号処理をテストと実装で確認。独立実走後にsnapshot残留なし。"
  refactoring:
    status: "done"
    note: "CSV解析、列検証、集計、可視化の責務分離と実ファイルテストを確認。立証した保守性欠陥なし。"
  assumption_conformance:
    status: "done"
    note: "観点1〜7完了後に id/assumption/target のみ読んで独立判定し、その後 reason/confidence/spec_refs を確認。23件を照合し追加unlisted候補なし。分類差1件。"
assumptions_sha256: "0d5632b7da2ce5b1fdd39ed9dc7d5625312319ba0e01ee29765873e01def5412"
assumption_verdicts:
  - id: "A-005"
    tier: "tier-facade"
    assumption: "ヘッダー行がクォート不正のときは、ヘッダー行とデータ行の csv quote is invalid だけを全件報告し、ヘッダー検証と列検証を行わずに終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:151"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-001"
    evidence: "tier-facade.md の UC ロジックはクォート不正の拒否を定めるが、ヘッダー解析不能時の後続検証範囲は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-006"
    tier: "tier-facade"
    assumption: "ヘッダー不一致(必須列の欠落、host / user の片方だけ)のときは、データ行の列検証と job_id 重複検査を行わず、データ行の csv quote is invalid だけを追加で報告して終了コード 2 で終える"
    target: "facade/src/usecase/validate_job_map.sh:158"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-002"
    evidence: "tier-facade.md のヘッダー検証と UC ロジックは欠落列の拒否を定めるが、列対応不能時にどの後続検証を省くかは定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-008"
    tier: "tier-facade"
    assumption: "job map header mismatch の missing= に複数の列を並べるときは、契約の列順(job_id, host, user, work_dir, script, fixed_params, hang_detect_limit_minutes)で並べる"
    target: "facade/src/domain/job_map.sh:150"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-003"
    evidence: "CLI 契約 column_matching は missing=<col,...> を定めるが、複数欠落列の列挙順は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-010"
    tier: "tier-facade"
    assumption: "csv quote is invalid または column count mismatch になったデータ行は、列ごとの検証、job_id 重複検査、map_version の集計の対象にしない"
    target: "facade/src/usecase/validate_job_map.sh:176"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-004"
    evidence: "tier-facade.md の列数検証とクォート規則は拒否を定めるが、解析不能行を重複検査や版集計へ含める方法は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-014"
    tier: "tier-facade"
    assumption: "info: resolved 行の fixed_params= は、解析後の要素を要素間に空白を置かない 1 行の JSON 配列に組み立て直し、スラッシュ(/)と U+00A0 以上の非 ASCII 文字はエスケープしない"
    target: "facade/src/domain/job_map.sh:420"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-005"
    evidence: "CLI 契約 conventions.output_format.control_chars と validate-config.sh.stderr は JSON 表記を求めるが、等価な JSON の空白とスラッシュ表記の選択は固定しない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-016"
    tier: "tier-facade"
    assumption: "hang_detect_limit_minutes が導入時の推奨値 60 と違う行に対する専用の info: 行は出さず、--verbose の info: resolved 行の hang_detect_limit_minutes=N で値を示すだけにする"
    target: "facade/src/domain/job_map.sh:509"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-006"
    evidence: "spec.md のバリエーション一覧は推奨値との差を info とするが、tier-facade.md 出力契約と CLI 契約に専用行の文言・出力条件はない。verbose の解決行で示す判断は未確定。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-017"
    tier: "tier-facade"
    assumption: "map_version の distinct 値が複数あるとき、stdout の map_version= と warn: mixed map_version の values= には、distinct 値を初出順のカンマ区切りで出す"
    target: "facade/src/domain/job_map.sh:655"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-007"
    evidence: "spec.md の計算ルール一覧は distinct 値と混在警告を定めるが、値の出現順での直列化は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-018"
    tier: "tier-facade"
    assumption: "map_version が空の行と値のある行が混在するとき、空の値は warn: mixed map_version の values= と stdout の map_version= に空値表記 - で出す(例: 空の行と v1 の行なら values=-,v1 と map_version=-,v1)"
    target: "facade/src/domain/job_map.sh:655"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-008"
    evidence: "spec.md の版の集計は全行空の場合の - を定めるが、空値と非空値の混在一覧内の空値表記は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-019"
    tier: "tier-facade"
    assumption: "--verbose の info: resolved 行は列ごとの検証を通過した行にだけ出し、違反のある行には出さない(他の行に違反があって終了コード 2 になるときも、通過した行の分は出す)"
    target: "facade/src/usecase/validate_job_map.sh:180"
    category: "data_format"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-009"
    evidence: "CLI 契約 validate-config.sh.stderr の info: resolved は正常行の形を定めるが、検証失敗時の有効行だけの診断出力は定めない。失敗時の部分結果の扱いなので error_handling と独立分類した。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-020"
    tier: "tier-facade"
    assumption: "stderr の行は、入力の守備範囲の error(行番号順。hint は最後の CR の次)、ヘッダーの unknown column の warn、duplicate column の error、header mismatch の error、データ行ごとの error と warn と info(行番号の昇順)、duplicate job_id、mixed map_version の順に、検証を終えてからまとめて出す"
    target: "facade/src/usecase/validate_job_map.sh:129"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-010"
    evidence: "tier-facade.md の検証順序と CLI 契約の内部障害時の出力抑制は明示されるが、各種 stderr 行相互の表示順の全体は固定されない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-022"
    tier: "tier-facade"
    assumption: "job_id 重複検査は、job_id is invalid になった値も文字列(バイト列の完全一致)として比較の対象に含め、空の job_id だけを対象から外す"
    target: "facade/src/usecase/validate_job_map.sh:189"
    category: "input_validation"
    verified_category: "input_validation"
    verdict: "spec_absent"
    finding_id: "F-011"
    evidence: "tier-facade.md の job_id 制約は形式と一意性を定めるが、形式違反値と空値について重複診断を重ねる範囲は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-025"
    tier: "tier-facade"
    assumption: "複数の job_id が重複しているとき、error: duplicate job_id の行は、その job_id が 1 回目に現れた行番号の昇順で出す"
    target: "facade/src/domain/job_map.sh:636"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-012"
    evidence: "CLI 契約 validate-config.sh.stderr の lines= 規則は一つのキー内の全出現行番号を定める。別の重複キー同士の error 行の順序は定めない。独立実走で B lines=2,4,5、A lines=3,6 の順を確認。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-043"
    tier: "tier-facade"
    assumption: "検証を通過した行の解決結果(info: resolved)や版の集計を作れない内部障害は、補助コマンドの失敗と同じ error: internal command failed commands=<関数名> path: <path> の 1 行と終了コード 6 で終える(commands= に外部コマンドではなく関数名 job_map_row_resolved_line / job_map_version_summary を入れる)"
    target: "facade/src/usecase/validate_job_map.sh:183"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-013"
    evidence: "CLI 契約 config_input_rules.internal_failure は複製工程と補助コマンド失敗を定めるが、内部関数名を commands= へ置く規則はない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-044"
    tier: "tier-facade"
    assumption: "補助コマンドが終了状態 0 を返しても、次のどれかに当たるときは補助コマンドの失敗として扱う。sort の出力の行数が入力の行数と違う。sort の出力が job_id のバイト列の昇順に並んでいない。事前走査の出力に最終行まで走査した証跡(証跡の文字と総行数)が無い・想定外の文字がある。事前走査が数えた総行数と bash が読み込んだ行数が違う"
    target: "facade/src/domain/job_map.sh:605"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-014"
    evidence: "CLI 契約 config_input_rules.internal_failure は想定外の出力を失敗として扱うが、行数・整列・走査証跡による検出条件の選択は実装に委ねられている。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-049"
    tier: "tier-facade"
    assumption: "ヘッダー列名の重複があるときは、duplicate column の error(重複した列名ごとに 1 行、初出順)に加えて必須列の欠落があれば header mismatch の error も同時に報告し、データ行の列検証と job_id 重複検査は行わずに終了コード 2 で終える"
    target: "facade/src/domain/job_map.sh:173"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-015"
    evidence: "CLI 契約 config_input_rules.rules は重複列名を拒否するが、欠落列との同時報告順と後続の行検証範囲は固定しない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-050"
    tier: "tier-facade"
    assumption: "入力の守備範囲の違反(NUL / 不正な UTF-8 / BOM / CR)が 1 行でもあるときは、その error 行(+ hint)だけを全件報告し、ヘッダー検証・行の検証・job_id 重複検査・版の集計は行わずに終了コード 2 で終える(違反の無い行の csv quote is invalid や列の違反も報告しない)"
    target: "facade/src/usecase/validate_job_map.sh:145"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-016"
    evidence: "tier-facade.md の BDD は NUL 時の quote 診断抑制と BOM 時の header 診断抑制を求めるが、別の正常バイト行の違反診断もすべて抑制する範囲は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-051"
    tier: "tier-facade"
    assumption: "同じ行に入力の守備範囲の違反が複数あるとき(例: NUL と CR を含む行)は原因ごとに error 行を出し、1 行の中では byte order mark → nul byte → encoding is not utf-8 → carriage return の順に並べる"
    target: "facade/src/usecase/validate_job_map.sh:47"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-017"
    evidence: "CLI 契約 config_input_rules.rules は各原因の診断と CR の hint 位置を定めるが、同じ行に複数原因がある場合の原因相互の順序は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-052"
    tier: "tier-facade"
    assumption: "mktemp がテンプレート末尾以外の X を置き換えない環境(BSD 系の開発機)では、テンプレートのままの名前で作られた作業ファイルを relay-gate-<pid>-<乱数>.part へ移してから使う(最終名は同じ規則で .part を外した名前。実行環境の Linux ではテンプレート relay-gate-XXXXXX.part をそのまま使う)"
    target: "facade/src/repository/job_map_repo.sh:333"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-018"
    evidence: "CLI 契約 config_input_rules.snapshot.validate_config は接頭辞・作業名・rename・0600 を定めるが、BSD mktemp の互換用ファイル名の具体形式は定めない。権限や保持方式の変更ではなく名前の表現の選択。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-053"
    tier: "tier-facade"
    assumption: "事前走査のパイプライン(tr | sed)の commands= は、sed が非 0 で終わり tr が SIGPIPE(終了状態 141)で終わったときは sed だけとし、sed の出力が想定外(原因の文字と数字以外・証跡なし)のときは sed、走査した総行数と読み込んだ行数が合わないときは tr,sed とする"
    target: "facade/src/repository/job_map_repo.sh:500"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-019"
    evidence: "CLI 契約 config_input_rules.internal_failure は実際に失敗したコマンドの表示を求めるが、SIGPIPE の原因帰属と行数不一致時の原因候補の表示規則は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-054"
    tier: "tier-facade"
    assumption: "warn: unknown column は未知の列名の出現ごとに 1 行出す(同じ未知の列名が 2 回あれば warn が 2 行と duplicate column の error が 1 行出る)"
    target: "facade/src/domain/job_map.sh:166"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-020"
    evidence: "CLI 契約 column_matching は未知列警告と重複列拒否を定めるが、未知の同名列の警告をまとめるかは定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-055"
    tier: "tier-facade"
    assumption: "読み込み後の複製の削除(rm)が失敗したときは、読み込み結果を検証結果として使わず、補助コマンドの失敗として error: internal command failed commands=rm path: <path> の 1 行と終了コード 6 で終える(stdout の map_path / rows / map_version は出さない)"
    target: "facade/src/repository/job_map_repo.sh:307"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "consistent"
    finding_id: "F-021"
    evidence: "CLI 契約 config_input_rules.snapshot.validate_config は読み込み後の削除を要求し、internal_failure は複製完了後の補助コマンド失敗を error 1 行・stdout なし・終了コード6と明示する。rm はこの明示規則に直接該当する。単体テスト172でも確認。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-056"
    tier: "tier-facade"
    assumption: "複製の工程(mktemp / cat / mv)の失敗と複製の削除(rm)の失敗が重なったときは、入力の複製の失敗(error: config snapshot failed + hint: check TMPDIR is writable、終了コード 6)として報告し、commands=rm の行は出さない"
    target: "facade/src/repository/job_map_repo.sh:307"
    category: "error_handling"
    verified_category: "error_handling"
    verdict: "spec_absent"
    finding_id: "F-022"
    evidence: "CLI 契約 config_input_rules.internal_failure は二つの障害分類を定めるが、複製失敗と削除失敗が重なった場合の報告優先順位は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "A-057"
    tier: "tier-facade"
    assumption: "複製完了後の補助コマンド(tr / sed)の失敗と複製の削除(rm)の失敗が重なったときは、commands= に失敗した順でカンマ区切りに並べる(例: commands=sed,rm)"
    target: "facade/src/repository/job_map_repo.sh:312"
    category: "data_format"
    verified_category: "data_format"
    verdict: "spec_absent"
    finding_id: "F-023"
    evidence: "CLI 契約 config_input_rules.internal_failure は commands=<name,...> を定めるが、複数名の列挙順は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
assumption_verdicts_summary:
  consistent: 1
  spec_absent: 22
  contradicts: 0
  unlisted: 0
findings:
  - id: "F-001"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-005"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:151"
    claim: "仕様で未確定の判断としてレビュー対象に残す。ヘッダー行がクォート不正のときは、ヘッダー行とデータ行の csv quote is invalid だけを全件報告し、ヘッダー検証と列検証を行わずに終了コード 2 で終える"
    evidence: "tier-facade.md の UC ロジックはクォート不正の拒否を定めるが、ヘッダー解析不能時の後続検証範囲は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-002"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-006"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:158"
    claim: "仕様で未確定の判断としてレビュー対象に残す。ヘッダー不一致(必須列の欠落、host / user の片方だけ)のときは、データ行の列検証と job_id 重複検査を行わず、データ行の csv quote is invalid だけを追加で報告して終了コード 2 で終える"
    evidence: "tier-facade.md のヘッダー検証と UC ロジックは欠落列の拒否を定めるが、列対応不能時にどの後続検証を省くかは定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-003"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-008"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:150"
    claim: "仕様で未確定の判断としてレビュー対象に残す。job map header mismatch の missing= に複数の列を並べるときは、契約の列順(job_id, host, user, work_dir, script, fixed_params, hang_detect_limit_minutes)で並べる"
    evidence: "CLI 契約 column_matching は missing=<col,...> を定めるが、複数欠落列の列挙順は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-004"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-010"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:176"
    claim: "仕様で未確定の判断としてレビュー対象に残す。csv quote is invalid または column count mismatch になったデータ行は、列ごとの検証、job_id 重複検査、map_version の集計の対象にしない"
    evidence: "tier-facade.md の列数検証とクォート規則は拒否を定めるが、解析不能行を重複検査や版集計へ含める方法は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-005"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-014"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:420"
    claim: "仕様で未確定の判断としてレビュー対象に残す。info: resolved 行の fixed_params= は、解析後の要素を要素間に空白を置かない 1 行の JSON 配列に組み立て直し、スラッシュ(/)と U+00A0 以上の非 ASCII 文字はエスケープしない"
    evidence: "CLI 契約 conventions.output_format.control_chars と validate-config.sh.stderr は JSON 表記を求めるが、等価な JSON の空白とスラッシュ表記の選択は固定しない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-006"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-016"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:509"
    claim: "仕様で未確定の判断としてレビュー対象に残す。hang_detect_limit_minutes が導入時の推奨値 60 と違う行に対する専用の info: 行は出さず、--verbose の info: resolved 行の hang_detect_limit_minutes=N で値を示すだけにする"
    evidence: "spec.md のバリエーション一覧は推奨値との差を info とするが、tier-facade.md 出力契約と CLI 契約に専用行の文言・出力条件はない。verbose の解決行で示す判断は未確定。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-007"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-017"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:655"
    claim: "仕様で未確定の判断としてレビュー対象に残す。map_version の distinct 値が複数あるとき、stdout の map_version= と warn: mixed map_version の values= には、distinct 値を初出順のカンマ区切りで出す"
    evidence: "spec.md の計算ルール一覧は distinct 値と混在警告を定めるが、値の出現順での直列化は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-008"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-018"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:655"
    claim: "仕様で未確定の判断としてレビュー対象に残す。map_version が空の行と値のある行が混在するとき、空の値は warn: mixed map_version の values= と stdout の map_version= に空値表記 - で出す(例: 空の行と v1 の行なら values=-,v1 と map_version=-,v1)"
    evidence: "spec.md の版の集計は全行空の場合の - を定めるが、空値と非空値の混在一覧内の空値表記は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-009"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-019"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:180"
    claim: "仕様で未確定の判断としてレビュー対象に残す。--verbose の info: resolved 行は列ごとの検証を通過した行にだけ出し、違反のある行には出さない(他の行に違反があって終了コード 2 になるときも、通過した行の分は出す)"
    evidence: "CLI 契約 validate-config.sh.stderr の info: resolved は正常行の形を定めるが、検証失敗時の有効行だけの診断出力は定めない。失敗時の部分結果の扱いなので error_handling と独立分類した。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-010"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-020"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:129"
    claim: "仕様で未確定の判断としてレビュー対象に残す。stderr の行は、入力の守備範囲の error(行番号順。hint は最後の CR の次)、ヘッダーの unknown column の warn、duplicate column の error、header mismatch の error、データ行ごとの error と warn と info(行番号の昇順)、duplicate job_id、mixed map_version の順に、検証を終えてからまとめて出す"
    evidence: "tier-facade.md の検証順序と CLI 契約の内部障害時の出力抑制は明示されるが、各種 stderr 行相互の表示順の全体は固定されない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-011"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-022"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:189"
    claim: "仕様で未確定の判断としてレビュー対象に残す。job_id 重複検査は、job_id is invalid になった値も文字列(バイト列の完全一致)として比較の対象に含め、空の job_id だけを対象から外す"
    evidence: "tier-facade.md の job_id 制約は形式と一意性を定めるが、形式違反値と空値について重複診断を重ねる範囲は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-012"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-025"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:636"
    claim: "仕様で未確定の判断としてレビュー対象に残す。複数の job_id が重複しているとき、error: duplicate job_id の行は、その job_id が 1 回目に現れた行番号の昇順で出す"
    evidence: "CLI 契約 validate-config.sh.stderr の lines= 規則は一つのキー内の全出現行番号を定める。別の重複キー同士の error 行の順序は定めない。独立実走で B lines=2,4,5、A lines=3,6 の順を確認。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-013"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-043"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:183"
    claim: "仕様で未確定の判断としてレビュー対象に残す。検証を通過した行の解決結果(info: resolved)や版の集計を作れない内部障害は、補助コマンドの失敗と同じ error: internal command failed commands=<関数名> path: <path> の 1 行と終了コード 6 で終える(commands= に外部コマンドではなく関数名 job_map_row_resolved_line / job_map_version_summary を入れる)"
    evidence: "CLI 契約 config_input_rules.internal_failure は複製工程と補助コマンド失敗を定めるが、内部関数名を commands= へ置く規則はない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-014"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-044"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:605"
    claim: "仕様で未確定の判断としてレビュー対象に残す。補助コマンドが終了状態 0 を返しても、次のどれかに当たるときは補助コマンドの失敗として扱う。sort の出力の行数が入力の行数と違う。sort の出力が job_id のバイト列の昇順に並んでいない。事前走査の出力に最終行まで走査した証跡(証跡の文字と総行数)が無い・想定外の文字がある。事前走査が数えた総行数と bash が読み込んだ行数が違う"
    evidence: "CLI 契約 config_input_rules.internal_failure は想定外の出力を失敗として扱うが、行数・整列・走査証跡による検出条件の選択は実装に委ねられている。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-015"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-049"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:173"
    claim: "仕様で未確定の判断としてレビュー対象に残す。ヘッダー列名の重複があるときは、duplicate column の error(重複した列名ごとに 1 行、初出順)に加えて必須列の欠落があれば header mismatch の error も同時に報告し、データ行の列検証と job_id 重複検査は行わずに終了コード 2 で終える"
    evidence: "CLI 契約 config_input_rules.rules は重複列名を拒否するが、欠落列との同時報告順と後続の行検証範囲は固定しない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-016"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-050"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:145"
    claim: "仕様で未確定の判断としてレビュー対象に残す。入力の守備範囲の違反(NUL / 不正な UTF-8 / BOM / CR)が 1 行でもあるときは、その error 行(+ hint)だけを全件報告し、ヘッダー検証・行の検証・job_id 重複検査・版の集計は行わずに終了コード 2 で終える(違反の無い行の csv quote is invalid や列の違反も報告しない)"
    evidence: "tier-facade.md の BDD は NUL 時の quote 診断抑制と BOM 時の header 診断抑制を求めるが、別の正常バイト行の違反診断もすべて抑制する範囲は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-017"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-051"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:47"
    claim: "仕様で未確定の判断としてレビュー対象に残す。同じ行に入力の守備範囲の違反が複数あるとき(例: NUL と CR を含む行)は原因ごとに error 行を出し、1 行の中では byte order mark → nul byte → encoding is not utf-8 → carriage return の順に並べる"
    evidence: "CLI 契約 config_input_rules.rules は各原因の診断と CR の hint 位置を定めるが、同じ行に複数原因がある場合の原因相互の順序は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-018"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-052"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:333"
    claim: "仕様で未確定の判断としてレビュー対象に残す。mktemp がテンプレート末尾以外の X を置き換えない環境(BSD 系の開発機)では、テンプレートのままの名前で作られた作業ファイルを relay-gate-<pid>-<乱数>.part へ移してから使う(最終名は同じ規則で .part を外した名前。実行環境の Linux ではテンプレート relay-gate-XXXXXX.part をそのまま使う)"
    evidence: "CLI 契約 config_input_rules.snapshot.validate_config は接頭辞・作業名・rename・0600 を定めるが、BSD mktemp の互換用ファイル名の具体形式は定めない。権限や保持方式の変更ではなく名前の表現の選択。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-019"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-053"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:500"
    claim: "仕様で未確定の判断としてレビュー対象に残す。事前走査のパイプライン(tr | sed)の commands= は、sed が非 0 で終わり tr が SIGPIPE(終了状態 141)で終わったときは sed だけとし、sed の出力が想定外(原因の文字と数字以外・証跡なし)のときは sed、走査した総行数と読み込んだ行数が合わないときは tr,sed とする"
    evidence: "CLI 契約 config_input_rules.internal_failure は実際に失敗したコマンドの表示を求めるが、SIGPIPE の原因帰属と行数不一致時の原因候補の表示規則は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-020"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-054"
    severity: "minor"
    target: "facade/src/domain/job_map.sh:166"
    claim: "仕様で未確定の判断としてレビュー対象に残す。warn: unknown column は未知の列名の出現ごとに 1 行出す(同じ未知の列名が 2 回あれば warn が 2 行と duplicate column の error が 1 行出る)"
    evidence: "CLI 契約 column_matching は未知列警告と重複列拒否を定めるが、未知の同名列の警告をまとめるかは定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-021"
    viewpoint: "assumption_conformance"
    kind: "restatement"
    assumption_id: "A-055"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:307"
    claim: "既定の契約規則の復唱を前提として記録している。読み込み後の複製の削除(rm)が失敗したときは、読み込み結果を検証結果として使わず、補助コマンドの失敗として error: internal command failed commands=rm path: <path> の 1 行と終了コード 6 で終える(stdout の map_path / rows / map_version は出さない)"
    evidence: "CLI 契約 config_input_rules.snapshot.validate_config は読み込み後の削除を要求し、internal_failure は複製完了後の補助コマンド失敗を error 1 行・stdout なし・終了コード6と明示する。rm はこの明示規則に直接該当する。単体テスト172でも確認。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-022"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-056"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:307"
    claim: "仕様で未確定の判断としてレビュー対象に残す。複製の工程(mktemp / cat / mv)の失敗と複製の削除(rm)の失敗が重なったときは、入力の複製の失敗(error: config snapshot failed + hint: check TMPDIR is writable、終了コード 6)として報告し、commands=rm の行は出さない"
    evidence: "CLI 契約 config_input_rules.internal_failure は二つの障害分類を定めるが、複製失敗と削除失敗が重なった場合の報告優先順位は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-023"
    viewpoint: "assumption_conformance"
    kind: "spec_absent"
    assumption_id: "A-057"
    severity: "minor"
    target: "facade/src/repository/job_map_repo.sh:312"
    claim: "仕様で未確定の判断としてレビュー対象に残す。複製完了後の補助コマンド(tr / sed)の失敗と複製の削除(rm)の失敗が重なったときは、commands= に失敗した順でカンマ区切りに並べる(例: commands=sed,rm)"
    evidence: "CLI 契約 config_input_rules.internal_failure は commands=<name,...> を定めるが、複数名の列挙順は定めない。 S4 固定指示にも追加の確定規則はない。実装の対象箇所と照合済み。"
  - id: "F-024"
    viewpoint: "assumption_conformance"
    kind: "category_mismatch"
    assumption_id: "A-019"
    severity: "minor"
    target: "facade/src/usecase/validate_job_map.sh:180"
    claim: "検証NG時にどの行の部分結果を出すかの判断なので、data_format より error_handling が適切。"
    evidence: "CLI 契約 validate-config.sh.stderr の info: resolved は正常行の形を定めるが、検証失敗時の有効行だけの診断出力は定めない。失敗時の部分結果の扱いなので error_handling と独立分類した。"
summary:
  blocker: 0
  major: 0
  minor: 24
environment_failures: []
verification_evidence:
  tdd_tests: 334
  bdd_scenarios: 41
  bdd_steps: 143
  target_bdd_scenarios: 32
  independent_credential_cases: 9
  nfr_5000_elapsed_seconds: 3.503
  environment: "macOS arm64 / bash 5.3.15; 実行に必要なコマンド・実ファイルI/O・子プロセスを確認。DB/TCPは当該テストの依存ではない。"
assumption_verdicts_sha256: "dce486c7e54e41ce1c3da016bf48fff4bb8e2e664a6ce168e563be9cf41f0db2"
