# S4 tier-impl 完了(eff24f55 / tier-facade / attempt 5)— 検証中のファイル差し替えの修正

- 検証開始時に入力を一時ファイルへ 1 回だけコピーし、バイト点検・空ファイル判定・CSV 読み込みはすべてそのコピーだけを読む(単一スナップショット)
- 一時ファイルは正常終了・検証エラー・補助コマンド失敗・HUP / INT / TERM のいずれでも削除する(KILL は対象外)
- 出力のパスはユーザー指定の元のパスのまま(一時ファイルのパスは出さない)
- コピーの失敗は既存の internal error 行と終了コード 6(一時ディレクトリに書けない環境では正しい CSV も終了コード 6 になる)
- 回帰テスト 10 件を先に追加し RED を確認後に修正。bats 287 件、tier BDD 23 Scenario が pass。既存出力は不変
- 前提は 34 件 → 38 件(単一スナップショットの判断、一時ファイルの扱い(security)などを追加)
