# RDRA フィードバック（Spec 生成時の発見事項）

本ファイルは、Spec 生成時に「Spec 側だけでは解消できず、RDRA モデル（および USDM）側の見直しが必要」と判定した項目を記録する。

RDRA 要素単位のトレーサビリティ（`traceability-matrix.md`）は網羅率 100% だが、USDM acceptance criteria 単位の逆引き（`usdm-acceptance-matrix.md`）で 1 件のギャップが残った。要素網羅では検出できない性質のギャップであるため、ここに切り出す。

## 変更要望一覧

| # | 種別 | 対象 RDRA 要素 | 変更内容 | 理由 |
|---|------|---------------|---------|------|
| 1 | 追加 | 情報: 比較定義（未定義） | job_id ごとの比較対象・比較実装を保持する情報エンティティを追加する。属性の候補は job_id / 比較対象テーブル / 比較対象ファイル / 比較実装識別子 / 有効期間 | USDM SPEC-012-03「比較定義は job_id ごとに差し替えられる」の acceptance criterion（Given job_id ごとに異なる比較対象・比較実装を定義する / When 速報・確報クロスチェックが実行される / Then job_id に応じた比較定義が適用される）を検証する Scenario を書けない。比較定義に相当する情報が RDRA 情報モデルに存在せず、Spec 側で発明すると RDRA 整合性ルールに違反するため |

## 補足（Spec 側で対応済みのため要望に含めないもの）

Spec 生成時に未対応だった他 10 件の acceptance criteria（SPEC-001-04 / 002-02 / 003-01 / 003-03 / 007-01#2 / 008-02#2 / 008-04 / 009-02 / 009-04 / 010-02 / 012-01）は、既存 UC の spec.md へ Scenario を追加して解消済みである。RDRA モデルの変更は不要。

## 対応方針

- 本フィードバックを `dist-requirements` で差分更新し、比較定義の情報エンティティを RDRA 情報モデルへ追加したうえでパイプラインを再実行する必要がある
- 再実行までの間、SPEC-012-03 は「仕様上の要求は存在するが検証可能な Scenario が未定義」として扱う。実装フェーズで比較定義の差し替えを実装対象に含めない
- 本項目は今回の feedback request 20260818_113601_impl_feedback_6078c4ed のスコープ外の発見事項であり、別の feedback request として起票する
