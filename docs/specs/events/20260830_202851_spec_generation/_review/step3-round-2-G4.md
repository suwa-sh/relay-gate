# Step3 Round 2 レビュー(G4: 監視調整 + 復旧、横断整合)

- 対象: hang_detect_limit_minutes をジョブごとに調整する / 実行中止フロー(2 UC + buc-spec)/ background 側リランフロー(4 UC + buc-spec)
- 参照: cli-command-contract.yaml / asyncapi.yaml / rdb-schema.yaml / ui-design.md / data-visualization.md / _inference.md / RDRA tsv / 方針資料「RelayGate のしくみ」/ 関連 UC(実装切替ジョブ実行フロー、両系成功時に速報比較依頼を作成する、比較ツールでジョブ単位比較を実行して結果を登録する、background 実行の経過時間と終了状態を判定する)
- 検査済みで問題なし: mermaid 構文(19 ファイル、md-mermaid-lint 全 OK)/ tier-*.md に画面・コンポーネント・screens・Storybook 参照なし / 固有名(製品名・サーバ名・業務名)なし / 方針資料との矛盾(abort は状態更新のみ・プロセス停止しない、リランは元 execution-spec.json 復元 + 新 run_id + parent_run_id、RAPID_CROSSCHECK_MODE=off 時の abort は管理 DB なしで 3 = _inference.md #7)なし / 状態 enum(slot_executions / parallel_runs / rapid_crosscheck_requests / final_crosscheck_requests / monitor_records)と列名は rdb-schema.yaml と一致 / rerun が起動する runner IF(`--execution-spec` + `--mode background`、PARAM 併用不可、run_id/job_id 一致必須、started-at.txt 既存は 2)は契約と一致 / rerun の依頼作成(run_id 主キー 1 件、REQUESTED、worker_id/lease_until NULL)は dispatcher UC の冪等条件(`INSERT ... WHERE NOT EXISTS` + 主キー)と両立

## 指摘一覧

| # | severity | ファイルパス(E 相対) | 行または節 | 指摘内容 | 具体的な修正案 |
|---|---|---|---|---|---|
| 1 | major | 実行復旧業務/background 側リランフロー/速報比較依頼だけを新規作成する/tier-ops.md, tier-rapid-crosscheck.md, spec.md | tier-ops.md L5「新しい成果物ディレクトリも作らない」/ BDD「runner も成果物ディレクトリも作らない」(`facade/{新 run_id}/ ディレクトリは存在しない`)、tier-rapid-crosscheck.md L43「新 run_id の成果物ディレクトリは存在しない」、spec.md BDD L178 | 他 UC「比較ツールでジョブ単位比較を実行して結果を登録する」tier-rapid-crosscheck.md L31 と契約 external_interfaces / artifact_layout では、worker が claim した依頼の run_id で `facade/<run_id>/rapid-crosscheck/{started-at.txt,stdout.log,stderr.log,exitcode.txt}` を書く。再作成依頼の run_id は新 run_id なので、比較実行後に `facade/<新 run_id>/rapid-crosscheck/` は必ず存在する。「存在しない」は worker 実行後の状態と矛盾し、tier BDD の Then(ディレクトリが存在しない)は比較実行のタイミング次第で false になる。加えて hang-detector は role=rapid-crosscheck の成果物パスを run_id から導出し(メール本文 artifact_dir)、parallel_runs.execution_spec_uri は元 run の spec(run_id が異なる)を指す | (a) 「background-rerun 自身は成果物ディレクトリを作らない。比較実行時に worker が `facade/<新 run_id>/rapid-crosscheck/` を作る(execution-spec.json と blue / green ディレクトリは持たない)」に書き換え、BDD Then を「background-rerun 終了直後に `facade/{新 run_id}/` が存在しない」に限定する。(b) tier-rapid-crosscheck.md L43 を「blue / green の成果物は元 run のもの。rapid-crosscheck 成果物は新 run_id 配下」に修正。(c) hang-detector UC(他 G3)が execution-spec.json の無い run ディレクトリ(blue/green started-at.txt なし)を走査対象外にする規則(tier-ops.md 走査手順 1)を、本 UC spec.md のクラッシュ耐性/関連 UC 注記として引用する |
| 2 | major | 実行復旧業務/実行中止フロー/実行を ABORTED へ遷移させる/tier-ops.md, spec.md; _cross-cutting/api/cli-command-contract.yaml | tier-ops.md「スクリプトごとの対象と条件付き UPDATE」共通(併更新)行、spec.md 処理フロー L82「更新件数(0 でも可。COMMIT)」、contract abort-rapid-crosscheck.sh exit 0 meaning「(parallel_runs も ABORTED)」 | 速報比較依頼が RUNNING の時点では、通常 run の parallel_runs は foreground 中継完了で既に COMPLETED になっている(rdb-schema.yaml parallel_runs.status)。併更新の WHERE `status IN ('STARTED','RUNNING')` は 0 件になり、契約の「parallel_runs も ABORTED」は通常 run では成立しない。一方、rerun(--role rapid-crosscheck)で作られた run は parallel_runs が RUNNING のままなので ABORTED に変わる。同じ abort-rapid-crosscheck.sh で parallel_runs の結果が run の出自で変わるが、その規則がどこにも明文化されていない。また rerun-rapid の parallel_runs は COMPLETED 遷移が未定義(spec 状態遷移一覧に rdra-feedback 注記あり)のため、run-lineage.sh に永久に RUNNING として現れる | (a) contract abort-rapid-crosscheck.sh の exit 0 meaning を「parallel_runs が STARTED / RUNNING なら併せて ABORTED(COMPLETED は変更しない)」に修正し、tier-ops.md 併更新行と spec.md 状態遷移一覧「並行稼働実行」の事前条件に「COMPLETED の parallel_runs は更新しない(0 件で可)」を明記する。(b) rerun-rapid の parallel_runs 終端(依頼 SUCCEEDED / FAILED 時に worker が COMPLETED にするか、依頼のみで parallel_runs を作らないか)を rdra-feedback に起票済みなら、速報比較依頼だけを新規作成する/spec.md とリラン結果を parent_run_id で追跡する/spec.md の双方に「現状は RUNNING のまま残る」旨を運用注記として揃える |
| 3 | major | _cross-cutting/api/cli-command-contract.yaml | commands: background-rerun.sh(L600 stderr)/ run-lineage.sh(L807 stderr)/ hang-detect-trend.sh(L564 exit 2 condition) | 契約の stderr 一覧が tier md で定義したメッセージを網羅していない(契約は機械可読な正本)。background-rerun.sh: `error: role is not supported by background-rerun run_id=... role=final-crosscheck`、`error: source request not found run_id=... role=rapid-crosscheck`、`error: run not found run_id=... role=...`、`error: execution-spec is not readable run_id=... path: ...`(6)、`warn: mode mismatch spec=... db=...`、`hint: abort the run with abort-{role}.sh ...` / `hint: request is still queued; wait for the worker` が欠落。run-lineage.sh: `error: lineage cycle detected run_id=...`(6)、`info: artifact_dir: ... run_id=...`(--verbose)が欠落。hang-detect-trend.sh: exit 2 条件に tier-ops.md の「job_id 形式不正」が無い | 各 tier md の出力契約表を正として契約 stderr / exit_codes.condition に転記する(リラン対象を検証する/tier-ops.md「出力契約」表の全行、リラン結果を parent_run_id で追跡する/tier-ops.md L58・L20、hang_detect_limit_minutes をジョブごとに調整する/tier-ops.md L40) |
| 4 | minor | 実行復旧業務/実行中止フロー/現在状態を確認して停止確認に応答する/tier-ops.md, spec.md; 実行を ABORTED へ遷移させる/spec.md, tier-ops.md | tier-ops.md BDD L102-104、spec.md 側 BDD L194-196 / L209-211、tier-ops.md BDD L137 | final_crosscheck_id の例が `20260830T020000Z-FINAL-1a2b3c4d`(大文字 FINAL)。契約 shared_rules.final_crosscheck_id の形式は `{UTC}-final-{8 hex}`(小文字)、asyncapi.yaml の pattern も `-final-`。他 UC(確報)の例は `20260830T210000Z-final-7b2c9e1f` | 例を `20260830T020000Z-final-1a2b3c4d` に統一する |
| 5 | minor | 実行復旧業務/実行中止フロー/現在状態を確認して停止確認に応答する/tier-ops.md; background 側リランフロー/リラン対象を検証する/tier-ops.md; _cross-cutting/api/cli-command-contract.yaml | abort tier-ops.md BDD L113 `error: option required option=--run-id`、rerun tier-ops.md BDD L118 `error: option required option=--role`、contract rapid-crosscheck-runner.sh L309 `error: missing option option=...` | 必須オプション欠落メッセージの語彙が UC 間で不一致(`option required` vs `missing option`)。ui-design.md L120 は `required` を優先語彙としているが、契約側の例は `missing option` | ui-design.md「メッセージ表現規約」に必須オプション欠落の定型文を 1 つ定め(推奨: `error: option required option=--x`)、契約 L309 と G2 UC を合わせる |
| 6 | minor | 実行復旧業務/実行中止フロー/現在状態を確認して停止確認に応答する/spec.md | BDD L167-171(Given に started_at が無いが Then で `started_at=2026-08-30T11:30:05Z` を検証)、L177-179(Given に started_at が無いが Then で `started_at=2026-08-30T11:46:00Z` を検証) | Then の具体値が Given に無く検証できない | Given に `started_at=2026-08-30T11:30:05Z` / `started_at=2026-08-30T11:46:00Z` を追加する |
| 7 | minor | _cross-cutting/api/cli-command-contract.yaml; 実行復旧業務/実行中止フロー/現在状態を確認して停止確認に応答する/tier-ops.md | contract artifact_layout.files started-at.txt readers「abort-*(現在状態表示)」、tier-ops.md 出力契約 行 7 `started_at` 出典 = slot_executions.started_at | 契約は abort-* が started-at.txt を読むとし、UC は slot_executions.started_at を読む。読み手が違う(値は同一時刻のはずだが精度・出典が二重化) | UC 側(slot_executions.started_at)を正として契約 readers から abort-* を外す。または UC 側に「off では管理 DB が無いため成果物も読まない(3 で終了)」を明記して契約と揃える |
| 8 | minor | _cross-cutting/api/asyncapi.yaml; 実行復旧業務/background 側リランフロー/速報比較依頼だけを新規作成する/tier-rapid-crosscheck.md | asyncapi RapidCrosscheckRequest schema L435-445(`blue_artifact_uri` / `green_artifact_uri` / `parent_run_id`)、tier-rapid-crosscheck.md L52「payload は依頼レコードそのもの」 | rdb-schema.yaml の rapid_crosscheck_requests にはこれら 3 列が無い(rapid_runs / parallel_runs の列)。「依頼レコードそのもの」と言いながら payload が依頼テーブルの列集合と一致しない | asyncapi の 3 プロパティを削除するか、description に「rapid_runs / parallel_runs から JOIN した派生値(列ではない)」と明記する。tier-rapid-crosscheck.md L52 は「依頼レコードの列 + 参照先の成果物 URI」に修正 |
| 9 | minor | 実行監視業務/background 実行監視フロー/hang_detect_limit_minutes をジョブごとに調整する/tier-facade.md | 出力契約 L25 `job_map: {path}` と `rows={N}` | 他 UC「slot ごとのジョブマップを定義する」tier-facade.md L58 と契約 validate-config.sh --job-map の stdout は `map_path: ...` / `rows=` / `map_version=` / `impl_version=` の 4 行 | `map_path: {path}` / `rows={N}` / `map_version=` / `impl_version=` に修正する。stderr 例 `error: invalid hang_detect_limit_minutes job_id=... value=... map=...` も当該 UC の定義と照合して揃える |
| 10 | minor | 実行監視業務/background 実行監視フロー/hang_detect_limit_minutes をジョブごとに調整する/spec.md | BDD L165-175 | Given の表に `hang_suspected_at` と `judged_at` が無いが、計算ルールでは `hang_suspected_count = COUNT(hang_suspected_at IS NOT NULL)`、`run_count` は `judged_at >= since` で絞る。Then の `hang_suspected_count=2` / `run_count=3` を Given から導出できない | Given 表に `hang_suspected_at`(1・2 行目に値、3 行目 `-`)と `judged_at`(3 行とも since 以降)列を追加する |
| 11 | minor | 実行復旧業務/background 側リランフロー/buc-spec.md | データフロー図 L37「新 run の実行設定\n(run_id のみ書き換え)」、CRUD 補足 L84「元 spec の run_id だけを書き換えて」 | UC spec / tier-ops / 契約 execution_spec_rules は run_id / parent_run_id / restored_at の 3 キーを書き換える | 「run_id / parent_run_id / restored_at を書き換え」に修正する。契約 execution_spec_example にも `restored_at`(通常起動は null)を追加すると C1 との対応が読める |
| 12 | minor | 実行復旧業務/background 側リランフロー/元の execution-spec.json から復元して新しい run_id で起動する/tier-facade.md, _api-summary.yaml | tier-facade.md 出力契約 L28「runner 自身のエラーは stderr に `error:` 1 行」、BDD L97 / L102(stderr に error が出る)、_api-summary.yaml L67 | 契約 `$BLUE_RUNNER / $GREEN_RUNNER` の stderr は「なし。SSH 失敗・spec 不整合は stderr.log 末尾に `error:` 1 行」。background-rerun は runner を非同期起動するため runner の stderr は誰にも届かない | 出力先を `facade/<run_id>/<role>/stderr.log` 末尾に統一し、BDD Then を「stderr.log 末尾に `error: execution-spec run_id mismatch ...`、exitcode.txt=2」に修正する |
| 13 | minor | 実行復旧業務/background 側リランフロー/元の execution-spec.json から復元して新しい run_id で起動する/spec.md, tier-ops.md; _cross-cutting/api/cli-command-contract.yaml | spec.md 異常系 BDD L214-220「runner 起動失敗時も成果物を残す」(exitcode.txt に非 0)、tier-ops.md 手順 4「3 ファイルは runner / gateway が可能な限り揃える」、contract artifact_layout exitcode.txt content「2 = runner 側検証エラー、6 = SSH / 書き込み失敗」 | runner 実体に実行権限が無い場合は runner が 3 ファイルを書けない。誰が(background-rerun の gateway)、どの値(2 か 6 か)で exitcode.txt を書くかが未定義。値が決まらないと hang-detector の判定(非 0 = background-exec-error 通知)と BDD の「非 0」検証が曖昧 | tier-ops.md 手順 4 に「起動不能時は background-rerun の gateway が started-at.txt / 空の stdout.log / 失敗理由の stderr.log / exitcode.txt=6 を書く」と明記し、契約 exitcode.txt content に「6 = SSH / 書き込み失敗 / runner 起動失敗(background-rerun が書く)」を追記。BDD Then を `exitcode.txt の中身が 6` にする |
| 14 | minor | 実行復旧業務/background 側リランフロー/リラン対象を検証する/tier-ops.md | 元状態の解決 L65「rapid-crosscheck は off では依頼が存在しないため `request_not_found`」、出力契約表 L35 の hint「rapid crosscheck request is created only when both slots succeeded」 | off 時に `source request not found` + 上記 hint を出すと、運用者は「両系成功していない」と誤読する。abort-* の off 時は `error: management db is not configured (RAPID_CROSSCHECK_MODE=off)` で統一されており、同じ状況で別メッセージになる | off かつ `--role rapid-crosscheck` は管理 DB 接続前に `error: management db is not configured (RAPID_CROSSCHECK_MODE=off) run_id=... role=rapid-crosscheck`(3)を返す行を出力契約表と事前検証表に追加し、契約 stderr にも転記する |
| 15 | minor | 実行復旧業務/実行中止フロー/現在状態を確認して停止確認に応答する/spec.md; 実行を ABORTED へ遷移させる/spec.md | 関連 RDRA モデル表 | 分岐条件一覧・ビジネスルールで参照している RDRA 条件が関連 RDRA モデル表に無い: 前半 UC「速報クロスチェック有効判定」、後半 UC「速報クロスチェック有効判定」「監視は通知のみ」 | 関連 RDRA モデル表の「条件」行に追加する(トレーサビリティ集計の分母・分子を揃える) |
| 16 | minor | 実行復旧業務/background 側リランフロー/元の execution-spec.json から復元して新しい run_id で起動する/spec.md | 関連 RDRA モデル表 | ビジネスルール(tier-ops.md L114)で参照する条件「認証情報の非保存」と、バリエーション一覧で参照する「速報クロスチェックモード」が関連 RDRA モデル表に無い | 条件「認証情報の非保存」、バリエーション「速報クロスチェックモード」を関連 RDRA モデル表に追加する |
| 17 | minor | 実行復旧業務/background 側リランフロー/リラン対象を検証する/tier-ops.md; 実装切替業務/実装切替ジョブ実行フロー/実装スクリプトを実行して Runner Result を出力する/tier-facade.md(他 G1) | verify tier-ops.md 元状態の解決(off)L65、他 UC tier-facade.md L44「started-at.txt だけがある場合は上書きして再実行する」 | 契約 idempotency は「通常起動では上書き再実行、復元起動では 2」と両経路を区別するが、G1 UC 側は復元起動の区別を書いていない。本 UC の tier-facade.md(L44)は「started-at.txt 既存は 2」で契約と一致 | G1 UC「実装スクリプトを実行して Runner Result を出力する」tier-facade.md L44 に「`--execution-spec` 指定時は started-at.txt 既存を二重起動として 2」を追記する(本 G4 の修正対象ではないが横断整合として記録) |

## 集計

| severity | 件数 |
|---|---|
| blocker | 0 |
| major | 3(#1 rerun-rapid の成果物ディレクトリの矛盾 / #2 abort-rapid と rerun-rapid の parallel_runs 併更新の意味の不整合 / #3 契約 stderr・exit 条件の欠落) |
| minor | 14 |
