# Step3 Round 2 レビュー(G3: 確報クロスチェックフロー 5 UC + background 実行監視フロー 4 UC)

- 観点: 横断契約(cli-command-contract / asyncapi / rdb-schema / ui-design)との整合、RDRA トレーサビリティ、BDD の検証可能性、summary yaml の整合、mermaid 構文、方針資料との矛盾
- mermaid: 対象 20 md を `md-mermaid-lint` で検証し、全件 OK
- design_available: false — tier md に画面・コンポーネント・Storybook 参照なし(「画面」行はすべて「→ CLI 出力 / 通知メール」の読み替えで問題なし)
- 集計: blocker 0 / major 4 / minor 12

パスは E = `docs/specs/events/20260830_202851_spec_generation` からの相対。

| # | severity | ファイル | 行・節 | 指摘内容 | 修正案 |
|---|---|---|---|---|---|
| 1 | major | E/実行監視業務/background 実行監視フロー/background 実行の経過時間と終了状態を判定する/_model-summary.yaml, tier-ops.md, ハング疑い・実行エラー・比較異常を通知する/spec.md | _model-summary `rapid_crosscheck_requests` の where(L88)、monitor_records の where(L118)/ tier-ops「走査手順 3」/ 通知 spec「冪等判定」 | 速報比較依頼の走査 SQL は、終端(SUCCEEDED / FAILED)の依頼のうち monitor_status が `HANG_SUSPECTED_NOTIFIED` の run を除外している。一方、同ファイルの monitor_records 走査は `HANG_SUSPECTED_NOTIFIED` を未終端として扱う(自己矛盾)。結果として「RUNNING で上限超過 → warning 通知後に SUCCEEDED / FAILED へ進んだ依頼」は二度と判定されず、(a) 比較 NG / FAILED でも `rapid-crosscheck-error` メールが送られない、(b) SUCCEEDED + OK でも COMPLETED にならず監視記録が永久に未終端のまま残る。通知 UC の遷移表(HANG_SUSPECTED_NOTIFIED → EXEC_ERROR_NOTIFIED のみ)にも HANG_SUSPECTED_NOTIFIED → COMPARE_ERROR_NOTIFIED / COMPLETED(依頼)が無い | 走査 SQL の除外リストから `HANG_SUSPECTED_NOTIFIED` を外す(未終端として再判定する)。通知 UC の冪等判定に「HANG_SUSPECTED_NOTIFIED かつ判定 COMPARE_ERROR → rapid-crosscheck-error を 1 回送り COMPARE_ERROR_NOTIFIED へ」を追加し、判定 UC の状態遷移一覧に「HANG_SUSPECTED_NOTIFIED → COMPLETED(依頼が SUCCEEDED かつ OK)」を明記する。状態.tsv には HANG_SUSPECTED_NOTIFIED → COMPARE_ERROR_NOTIFIED が無いため rdra-feedback.md に追記する。buc-spec.md の状態遷移全体図も同期する |
| 2 | major | E/クロスチェック業務/確報クロスチェックフロー/比較ツールで日次全量比較を実行して結果を保存する/tier-final-crosscheck.md, spec.md | tier md「比較ツールの起動」(L48)/ spec.md 計算ルール「比較ツール引数」(L134)/ 処理フロー(L86) | 比較ツールの起動形式が横断契約と一致しない。cli-command-contract.yaml の external_interfaces「比較ツール(確報)」は `compare_command + compare_options`(`{catalog_path}` / `{catalog_version}` / `{business_date}` をプレースホルダ置換)と定義し、validate-config も「プレースホルダ欠落」を検証する。tier md は `compare_command $compare_options --business-date <date> --target-catalog <tsv>` と固定オプションを追記する形、spec.md の処理フローは `--targets <catalog rows>` で、3 箇所が別々 | 契約どおり「`compare_options` 内の `{catalog_path}` / `{catalog_version}` / `{business_date}` を置換して起動する。固定オプションは追記しない」に統一する。spec.md 処理フローの `G->>Tool` 行も `compare_command {compare_options を置換した引数}` に修正する |
| 3 | major | E/実行監視業務/background 実行監視フロー/background 実行の経過時間と終了状態を判定する/spec.md | 計算ルール「exitcode.txt の解釈」(L156) | 整数でない exitcode.txt を `EXEC_ERROR` とし「exit_code=-1 を通知に載せる」としているが、asyncapi.yaml `HangAlertMail.exit_code` は `^([0-9]{1,3}\|-)$`(0〜999 または `-`)で `-1` を許さない。rdb-schema / 通知メール規約(ui-design.md「整数または `-`」)とも不整合 | 契約違反の exitcode.txt は `exit_code=-`(値なし)で通知し、実行ログに `WARN invalid exitcode run_id=... role=... value=...` を残す方式に変更する(通知 UC の本文行 9 の規約に合わせる)。あるいは asyncapi / ui-design.md 側で `-1` を許す場合はその 2 ファイルを先に改定する |
| 4 | major | E/クロスチェック業務/確報クロスチェックフロー/保存済みの確報結果をジョブスケジューラへ返す/spec.md | E2E 正常系「ABORTED は保存済み 3 値だけを返す(SPEC-006-03)」(L154-159) | Given「status=ABORTED exit_code=6 stdout="" stderr="killed by operator\n"」は到達不能状態。abort-final-crosscheck.sh は RUNNING → ABORTED の状態更新のみで exit_code / stderr を保存せず(cli-command-contract L742-781、方針資料「abort-final-crosscheck.sh」)、worker の終端 UPDATE は `WHERE status='RUNNING'` のため ABORTED 後に 3 値が保存されることもない。テスト不能な受け入れ条件になっている | シナリオを削除するか、「ABORTED で exit_code が NULL のとき終了コード 6」(異常系)に統合する。SPEC-006-03 の紐付けは「exit_code=3 の FAILED をそのまま返す」に残す |
| 5 | minor | E/クロスチェック業務/確報クロスチェックフロー/確報比較依頼を claim する/tier-final-crosscheck.md | 引数表(L19)/ UC ロジック「バリデーション」(L52) | `--worker-id` の許容文字が `^[A-Za-z0-9._-]{1,64}$`(`.` を含む)だが、cli-command-contract.yaml `final-crosscheck-worker.sh --worker-id` は「英数字・`_`・`-` のみ」 | 契約に合わせ `^[A-Za-z0-9_-]{1,64}$` にする(`{hostname}-{pid}` 既定値でホスト名に `.` が入る場合は契約側の改定を先に行う) |
| 6 | minor | E/実行監視業務/background 実行監視フロー/監視記録を保存する/_model-summary.yaml, tier-ops.md | indexes_needed(L84-86)/ tier md「インデックス」(L72) | 傾向集計用インデックスを `(job_id, role, alerted_at)` としているが、rdb-schema.yaml は `idx_monitor_records_job_id_role_judged_at` = `(job_id, role, judged_at)`(hang-detect-trend.sh の `--since` 絞り込みが judged_at のため) | `(job_id, role, judged_at)` に揃える |
| 7 | minor | E/実行監視業務/background 実行監視フロー/background 実行の経過時間と終了状態を判定する/spec.md, tier-ops.md | spec E2E「上限超過をハング疑いと判定する」(L216)/ tier md 実行ログ例(L50) | 実行ログの `judged` 行が spec.md では `INFO judged run_id=... role=green judgement=... elapsed_minutes=... limit_minutes=...`(job_id なし)、tier md では `INFO judged run_id=... role=... job_id=... judgement=...`(job_id あり)で形式が異なる。cli-command-contract の `--verbose` 説明は job_id なし | ログ行の固定形式を 1 つに決める(契約に合わせ job_id なし、または契約・tier・spec の 3 箇所を job_id ありに揃える) |
| 8 | minor | E/実行監視業務/background 実行監視フロー/background 実行の経過時間と終了状態を判定する/spec.md | 関連 RDRA モデル(L171-192) | _model-summary / データフローでは `comparison_results`(情報「比較結果(comparison_result)」)と `feature flag 設定` を読むが、関連 RDRA モデル表に両情報が無い | 情報行に「比較結果(comparison_result) — NG / FAILED の判定入力(on)」「feature flag 設定 — RAPID_CROSSCHECK_MODE の読み取り」を追加する |
| 9 | minor | E/クロスチェック業務/確報クロスチェックフロー/確報比較依頼を登録して終端状態まで待機する/spec.md | E2E「確報依頼を登録して SUCCEEDED まで待機する」(L186)、「polling 上限超過」(L209) | Then で `final_crosscheck_id=20260830T210000Z-final-7b2c9e1f` を完全一致で要求しているが、末尾 8 hex は乱数で起動時刻依存のため再現できない | 「`final_crosscheck_id=` に続く `^[0-9]{8}T[0-9]{6}Z-final-[0-9a-f]{8}$` 形式の値」のように形式一致に緩めるか、テストで ID を注入できる前提(環境変数など)を Given に書く |
| 10 | minor | E/クロスチェック業務/確報クロスチェックフロー/確報比較依頼を claim する/spec.md | E2E「REQUESTED の依頼を claim する(SPEC-006-02)」(L154-159) | `--once` は claim 後に同一プロセスで比較実行(次 UC)まで進むため、プロセス終了時点の状態は CLAIMED ではなく RUNNING / 終端になる。Then「status=CLAIMED になる」がそのままでは観測できない | Given に「クロスチェックジョブマップの確報行が未定義(または compare_command が待機するスタブ)」を置くか、Then を「実行ログの `claimed` 行と、lease_until=+10 分の記録」に限定する |
| 11 | minor | E/クロスチェック業務/確報クロスチェックフロー/確報クロスチェック結果を確認する/tier-final-crosscheck.md | 出力契約(L37) | 「終了コード 6 が relay-gate 由来かは stderr の先頭が `error: ` で `final_crosscheck_id=` を含むかで区別できる」としているが、ABORTED で exit_code NULL の場合は stderr に追加行を書かない(UC「保存済みの確報結果をジョブスケジューラへ返す」)ため区別できないケースがある | 「ABORTED で exit_code NULL の 6 は stderr が空(または保存済みのみ)になるため、実行ログ `WARN relay without stored exit_code` で確認する」と補足する |
| 12 | minor | E/クロスチェック業務/確報クロスチェックフロー/確報比較依頼を登録して終端状態まで待機する/tier-final-crosscheck.md, _api-summary.yaml | 設定契約(L30)/ _api-summary stderr(L31) | (a) `FINAL_POLL_LIMIT_SEC` の検証ルールが「`FINAL_POLL_INTERVAL_SEC` 以上」で、cli-command-contract(`integer(>=1)`)より厳しい。(b) _api-summary の stderr は「relay-gate 自身のエラーのみ」で、契約の「終端到達後は保存済み stderr をバイト列のまま出す」を含まない | (a) 契約に揃えるか、契約側の検証ルールを更新する。(b) _api-summary の stderr に無加工中継の記述を追加する(UC「保存済み結果を返す」への参照でも可) |
| 13 | minor | E/クロスチェック業務/確報クロスチェックフロー/比較ツールで日次全量比較を実行して結果を保存する/tier-final-crosscheck.md | UC ロジック「クラッシュ耐性」(L50) | 「hang-detector は確報依頼を走査しない(方針資料: 監視対象は background slot と速報比較依頼)」と根拠を方針資料に置いているが、方針資料 C2「Hang Detection」図には `HangDetector -.-> FinalQueue` の破線があり、C4 の本文とは食い違う。判定 UC 側の「final-crosscheck ディレクトリは走査しない」も同じ前提 | 根拠を「cli-command-contract.yaml hang-detector.sh / asyncapi.yaml(監視対象は slot + rapid)」に変え、方針資料 C2 図との差異は rdra-feedback.md(または todo)に「確報依頼のハング監視は runner の polling 上限(8 時間)で代替する仮採用」として記録する |
| 14 | minor | E/_cross-cutting/ux-ui/ui-design.md(参照整合) | 状態表示「ハング検知判定結果」(L151) | 判定 UC の tier md / cli-command-contract `hang_judgement` は `COMPARE_ERROR` を含む 5 値だが、ui-design.md の表は 4 値(`COMPARE_ERROR` なし)。`--verbose` 出力・ログに `judgement=COMPARE_ERROR` が現れるため横断規約側が欠落 | ui-design.md の行に `COMPARE_ERROR`(速報クロスチェック異常)を追加する(UC 側の修正は不要) |
| 15 | minor | E/実行監視業務/background 実行監視フロー/background 実行の経過時間と終了状態を判定する/_api-summary.yaml | exit_codes code 2(L28) | 終了コード 2 の条件に `ALERT_MAIL_TO` 空 / `ALERT_MAIL_CMD` 実行不可が無い(契約 L512 にはある。通知 UC の _api-summary には記載あり) | 判定 UC(defines 側)の exit_codes に追記する |
| 16 | minor | E/クロスチェック業務/確報クロスチェックフロー/buc-spec.md | 状態遷移全体図(L68-79) | 図に ABORTED が無い(本文で「実行復旧業務が担うため含めない」と断っている)が、UC1 の polling 終端条件と UC4 の中継条件は ABORTED を含むため、読者が終端 3 状態を図で追えない | 図に `RUNNING --> ABORTED: (実行中止フロー。別 BUC)` を注記付きで残すか、終端状態の凡例に ABORTED を明記する |

## 確認済み(指摘なし)

- コマンド名・引数・オプション・終了コード: `final-crosscheck-runner.sh` / `final-crosscheck-worker.sh` / `hang-detector.sh` の tier md は cli-command-contract.yaml と一致(#5 / #12 の軽微差異を除く)
- テーブル・列・状態 enum: `final_crosscheck_requests` / `monitor_records` の列名・型・状態値は rdb-schema.yaml と一致。monitor_records の `judged_at`(canonical C2)、`elapsed_minutes_at_alert` の命名も一致
- 非同期: `final-crosscheck-requests`(publish = runner INSERT / receive = worker claim・実行)、`hang-alert-mail`(件名・本文 12 行 + 空行 + recommended_action、role enum、冪等性)は asyncapi.yaml と一致。受信 UC の本文行順・定型文も送信 UC・ui-design.md と一致
- 他 UC 参照: hang-detector が参照する `slot_executions.status`(RUNNING / SUCCEEDED / FAILED / ABORTED)、`rapid_crosscheck_requests.status`(6 値)、`comparison_results.status`(OK / NG / FAILED)は各正本と一致。execution-spec.json の `slots.<role>.mode / hang_detect_limit_minutes / job_id` は契約の構造例と一致
- 方針資料: 確報 runner の「登録 → 終端まで同期 polling → 保存済み stdout / stderr / exitcode を無加工中継、状態名・差分件数・レポート URI を追加しない」、worker の 3 値保存、Runner Result(`.tmp` → `mv`)、実行履歴・監査はジョブスケジューラの責務、監視は通知のみ(ABORTED へ変更しない・プロセス停止しない・依頼を作らない)、固有名の中立表現 — いずれも矛盾なし
- RDRA トレーサビリティ: 条件.tsv の各 UC 紐付け条件(確報依頼の登録条件 / 確報結果の中継制約 / 速報と確報のモデル分離 / 確報クロスチェック非起動 / 依頼状態遷移規則 / claim 排他 / lease 失効判定 / 比較ツール終了コードの対応 / 適用側で定義する事項 / 成果物公開判定 / ハング検知判定 / ハング検知対象の除外 / 速報比較依頼の異常判定 / 速報クロスチェック有効判定 / 監視は通知のみ / 警告傾向の記録 / 通知レベルの判定 / CLI とメールによる提示 / 実行履歴はジョブスケジューラの責務 / 復旧手段の選択)は各 spec.md の分岐条件一覧・関連 RDRA モデルに反映済み。状態.tsv の監視状態 8 遷移は判定 UC / 通知 UC に分担され buc-spec の全体図と一致
