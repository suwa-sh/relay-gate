# Step3 Round 2 レビュー(G1: 実装切替ジョブ実行フロー 6 UC + 適用構成定義フロー 4 UC)

- 対象: `実装切替業務/実装切替ジョブ実行フロー/`(buc-spec + 6 UC)、`適用構成業務/適用構成定義フロー/`(buc-spec + feature flag / slot runner 割当 / slot ジョブマップ / 切り替えた運用モード)
- 突合先: `_cross-cutting/api/cli-command-contract.yaml` / `asyncapi.yaml` / `datastore/rdb-schema.yaml`、RDRA(`docs/rdra/latest/*.tsv`)、方針資料「RelayGate のしくみ」
- mermaid: `npx md-mermaid-lint` で対象 12 ファイル(spec.md × 10 + buc-spec.md × 2)を検証し全件 OK
- RDRA トレーサビリティ: BUC.tsv の情報・条件・画面・イベント・外部システム、状態.tsv の遷移を 10 UC すべて突合し、漏れなし(spec 側の追加採用条件は buc-spec に「(spec)」で明示済み)
- design_available=false: tier-*.md に画面仕様・コンポーネント・screens・Storybook の参照なし
- 方針資料との矛盾: なし(facade の foreground 待機と 3 ファイル無加工中継、runner への実装固有事項の閉じ込め、Runner Result 正本、RAPID_CROSSCHECK_MODE on/off、中立表現をすべて確認)

件数: blocker 0 / major 5 / minor 10

| # | severity | ファイルパス | 行または節 | 指摘内容 | 具体的な修正案 |
|---|---|---|---|---|---|
| 1 | major | `実装切替業務/実装切替ジョブ実行フロー/slot 実行モードを選択して runner を起動する/tier-facade.md` | L120(データモデル変更 › slot_executions) | 終了時刻列を `finished_at` と記述しているが、`rdb-schema.yaml` は `_review_notes` で UC 間の不一致を解消し `completed_at` に統一している(schema L26, L155-158)。契約と不一致 | `finished_at` → `completed_at` に改名し、説明を「SUCCEEDED / FAILED / ABORTED へ遷移した日時(UTC)」に揃える |
| 2 | major | `実装切替業務/実装切替ジョブ実行フロー/実装スクリプトを実行して Runner Result を出力する/spec.md`、同 `tier-facade.md`、同 `_model-summary.yaml` | spec.md L97(処理フロー SQL)、tier-facade.md L67(データモデル変更)・L115(ティア BDD)、_model-summary.yaml L59 | slot_executions の UPDATE 列を `finished_at` としている。`rdb-schema.yaml` の正本は `completed_at`。実装が契約生成物(packages/contracts)と食い違う | 4 箇所すべて `completed_at` に置換(`UPDATE slot_executions SET status=?, exit_code=?, completed_at=now WHERE run_id=? AND slot=? AND status='RUNNING'`)。BDD の Then も `completed_at が設定されている` にする |
| 3 | major | `実装切替業務/実装切替ジョブ実行フロー/実装スクリプトを実行して Runner Result を出力する/tier-facade.md` | L21(引数・オプション › `-- PARAM...`) | 復元起動(`--execution-spec`)でコマンドライン PARAM を「無視して WARN」としているが、`cli-command-contract.yaml` は `shared_rules.argument_concatenation.execution_spec`(L109)と runner IF の `-- PARAM...`(L228)・exit_codes 2(L266)で「PARAM 併用は受け付けない(終了コード 2)」と定義している。契約と矛盾 | 「復元起動で `--` 以降に PARAM があれば exitcode.txt=2 と stderr.log `error: params are not allowed with --execution-spec run_id=... role=...` で終了する」に改め、UC ロジックのバリデーション項目にも追加する。tier BDD に拒否シナリオを 1 本追加 |
| 4 | major | `適用構成業務/適用構成定義フロー/feature flag を設定する/tier-facade.md` | L55-66(出力契約 › stdout)、L107(ティア BDD「最終行は operation_mode=parallel」) | `validate-config.sh --feature-flag` の stdout を 8 行(`config_path`〜`operation_mode`)で「固定順」とし、BDD で `operation_mode` を最終行と断定している。しかし契約(L864)は 12 キー(`blue_job_map` / `green_job_map` / `blue_runner_if_version` / `green_runner_if_version` を後置)で固定順、UC「slot runner の実体スクリプトを割り当てる」tier-facade.md も同 4 行を追記すると定義している。同一コマンドの出力契約が UC 間で衝突し、BDD Then が常に失敗する | stdout 例を契約の 12 行に揃え、「runner 割当の 4 行は UC『slot runner の実体スクリプトを割り当てる』が定義」と注記する。BDD の Then を「stdout の 8 行目は `operation_mode=parallel`、最終行は `green_runner_if_version=1`(off の slot は `-`)」に修正 |
| 5 | major | `実装切替業務/実装切替ジョブ実行フロー/buc-spec.md` | L69(情報 CRUD マトリクス) | 表の途中(並行稼働実行の行と slot 実行の行の間)に段落「注記: execution_spec_uri は…」が挿入されており、Markdown の表が分断される。以降の 4 行(slot 実行 / 速報実行 / Runner Result / ジョブスケジューラ応答 / 実行ログ)が表として描画されない | 注記の段落を表の直後(L74 の後)へ移動する |
| 6 | minor | `実装切替業務/実装切替ジョブ実行フロー/ジョブマップで JOB_ID から実行先を解決する/spec.md` | L147・L153(E2E BDD「固定引数の後ろに PARAM を…」「空の固定引数は…」の When) | runner IF の必須オプション `--run-id` / `--role` / `--mode` を省いて `--job-id JOB001 -- a b` としている。契約では必須欠落は終了コード 2 のため、記述どおりでは検証不能 | `--run-id 20260830T113000Z-JOB001-3f9a1c2e --job-id JOB001 --role blue --mode foreground -- a b` のように必須オプションを明記する |
| 7 | minor | `実装切替業務/実装切替ジョブ実行フロー/ジョブマップで JOB_ID から実行先を解決する/spec.md` | L165・L171(異常系 BDD の stderr.log 期待値) | 期待メッセージに `/etc/relay-gate/green-job-map.tsv` を固定で書いているが、Given で `RELAY_GATE_CONFIG_DIR=/etc/relay-gate` を設定していない(契約の既定は `$RELAY_GATE_HOME/config`) | Given に「RELAY_GATE_CONFIG_DIR は /etc/relay-gate である」を追加するか、tier BDD と同様に `<RELAY_GATE_CONFIG_DIR>/green-job-map.tsv` と書く |
| 8 | minor | `実装切替業務/実装切替ジョブ実行フロー/業務ジョブの実行結果を確認する/spec.md` | L146(BDD「終了コードだけで成否を判定できる」の Then) | 「GUI を必要としない」は検証不能な表現 | 「終了コードは 4、標準エラーは `error: input file missing` である」で止め、GUI 不要は条件「CLI とメールによる提示」の説明側に置く |
| 9 | minor | `実装切替業務/実装切替ジョブ実行フロー/slot 実行モードを選択して runner を起動する/tier-facade.md` | L180(ティア BDD「runner 実体が実行不可」の Given) | `GREEN_MODE=background GREEN_RUNNER=...missing.sh` のみで `BLUE_MODE` / `RAPID_CROSSCHECK_MODE` が未指定。必須キー欠落は先に終了コード 2 になるため、どの検証で 2 になったか判別できない | Given を `BLUE_MODE=foreground BLUE_RUNNER=<実行可能> GREEN_MODE=background GREEN_RUNNER=/opt/relay-gate/runners/missing.sh RAPID_CROSSCHECK_MODE=off` にする |
| 10 | minor | `実装切替業務/実装切替ジョブ実行フロー/実装スクリプトを実行して Runner Result を出力する/tier-facade.md` | L44(UC ロジック › 冪等性) | 「started-at.txt だけがある場合は上書きして再実行」としか書かれていないが、契約の runner idempotency(L270)と exit_codes 2(L266)は「復元起動では 2(二重起動)」と区別している | 「通常起動では上書き再実行、`--execution-spec` 指定の復元起動では exitcode.txt=2(`error: restored run already started run_id=... role=...`)」を追記 |
| 11 | minor | `実装切替業務/実装切替ジョブ実行フロー/execution-spec.json を確定保存する/tier-facade.md` | L47(UC ロジック › バリデーション) | 既存 JSON の `run_id` 不一致・`slots` 欠落を「解析失敗 → 終了コード 6」としているが、契約の runner exit_codes 6(L269)は「lock タイムアウト・書き込み失敗」のみで解析失敗を含まず、2 の条件(L266)に「spec なし・run_id 不一致・欠落」がある。通常起動と復元起動で同じ事象が 2 / 6 に分かれ、実装者が迷う | 契約 L269 の exit 6 条件に「既存 execution-spec.json の解析失敗(通常起動)」を追記するか、tier 側を「2(設定検証エラー)」に寄せて統一する |
| 12 | minor | `適用構成業務/適用構成定義フロー/切り替えた運用モードで業務ジョブを実行する/_model-summary.yaml` | L28-32(`AlertMail` gateway) | 通知メールの gateway を tier-facade に置いているが、データフロー・tier-facade.md では facade はメールを送受信しない(publish は tier-ops の hang-detector、受信者は運用者)。tier md と不整合 | `AlertMail` を削除するか、`tier: "tier-ops"` / `layer: "gateway"` に変更して「参照のみ(tier-ops が定義)」と note を付ける |
| 13 | minor | `適用構成業務/適用構成定義フロー/slot ごとのジョブマップを定義する/_model-summary.yaml` | L38-42(`ApplicationDocument` repository) | 適用構成文書を repository record として列挙しているが、spec.md のバリエーション一覧・buc-spec CRUD は「relay-gate は読まない(R(間接))」としており、データフローに存在しない | 削除するか、`type: "reference"` 相当のコメントで「relay-gate は読み込まない。定義の根拠のみ」と明記する |
| 14 | minor | `実装切替業務/実装切替ジョブ実行フロー/ジョブマップで JOB_ID から実行先を解決する/tier-facade.md`、`適用構成業務/適用構成定義フロー/slot runner の実体スクリプトを割り当てる/_api-summary.yaml` | tier-facade.md L17-24(引数・オプション表)、_api-summary.yaml L58(`validate-config.sh` の `options: []`) | 契約の runner IF には `--help`(`runner-if-version=1` 応答)があるが、解決フェーズの引数表に無い。runner 割当 UC の `_api-summary.yaml` は `validate-config.sh --feature-flag` を uses としながら options が空 | 引数表に `--help` 行を追加。`_api-summary.yaml` の options に `--feature-flag`(required)・`--verbose` を列挙する |
| 15 | minor | `_cross-cutting/datastore/rdb-schema.yaml` | L363(`comparison_results.comparison_type` の description) | 値の例を「table / file / full」としているが、`cli-command-contract.yaml` の crosscheck-job-map.tsv `comparison_type` は enum `job` / `full`(L1014-1018)で、job 行を転記すると値は `job` になる。対象 UC 外だが横断契約同士の不一致 | description を「クロスチェックジョブマップの comparison_type(job / full。速報は job)」に修正 |
