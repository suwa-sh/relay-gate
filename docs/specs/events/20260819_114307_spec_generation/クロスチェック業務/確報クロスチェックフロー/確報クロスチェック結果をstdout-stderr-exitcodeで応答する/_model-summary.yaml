uc: "確報クロスチェック結果をstdout/stderr/exitcodeで応答する"
business: "クロスチェック業務"
buc: "確報クロスチェックフロー"

bounded_context_id: "BC-003"

models:
  - name: "FinalCrossCheckRespondTrigger"
    tier: "tier-worker"
    layer: "presentation"
    type: "request"
    rdra_info: "確報比較依頼"
  - name: "RespondFinalCrossCheckResultCommand"
    tier: "tier-worker"
    layer: "usecase"
    type: "command"
    rdra_info: "確報比較依頼"
  - name: "確報比較依頼"
    tier: "tier-worker"
    layer: "domain"
    type: "entity"
    rdra_info: "確報比較依頼"
  - name: "FinalCrossCheckRequestRecord"
    tier: "tier-worker"
    layer: "gateway"
    type: "record"
    rdra_info: "確報比較依頼"

tables:
  - name: "final_crosscheck_requests"
    rdra_info: "確報比較依頼"
    operations:
      - type: "SELECT"
        columns: []
        where: "run_id = ?"
    indexes_needed:
      - columns: ["run_id"]
        reason: "応答対象のrun_idからstatusを一意に取得するため（既存PKで充足するため追加インデックス不要）"
        access_pattern: "run_idによる単一レコード取得"

kvs: []
object_storage: []
