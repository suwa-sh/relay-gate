# AGENTS.md

## 必須事項

- 作業を開始する前に `git rev-parse --show-toplevel` で対象リポジトリのルートを確認すること。
- 対象リポジトリの branch と `git status --short` を確認し、別リポジトリや既存差分を取り違えないこと。
- 確認したリポジトリ直下の `CLAUDE.md` を必ず全文読み込み、その指示に従うこと。
